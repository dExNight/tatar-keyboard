# HANDOFF — релиз-кандидат 1.9.6 (кампания аудита закрыта)

**Состояние на 2026-08-31.** Кампания реструктуризации (1.9.5) закрыта раньше
(`docs/RESTRUCTURE.md`); поверх неё полностью закрыт полный аудит
`docs/AUDIT-2026-08-31.md` (все находки — с датированными пометками внутри).
Собран и проаудирован **1.9.6 / versionCode 22**:
`dist/tatar-keyboard-1.9.6.apk`, 2 105 064 Б, SHA-256
`b0f769441abe15c35b830fa86b970128abba509d99f6a88923f5e7567b61b9a3`,
подпись тем же ключом (`98ca6feb…42ad`). Аудит артефакта —
`docs/APK-AUDIT-1.9.6.md`. Наружу не ушло ничего: push, теги, публикация —
действия оператора (`docs/PUBLISH-CHECKLIST.md` перецелен на 1.9.6).

## Что вошло в 1.9.6 (12 коммитов фиксов аудита поверх 1.9.5)

- **U1+U2**: ₽ и «АБВ» на татарской раскладке, русские кавычки („“/«») —
  tt наследует TEXTS_ru;
- **U3**: TalkBack объявляет смену языка (только явные действия пользователя);
- **P2**: baseline-профиль покрывает движок подсказок (1560 → 2640 правил);
- **P1**: иконки лончера → lossless WebP (−29 КБ в исходниках);
- **P3**: dismiss-аниматор превью кэширован — ноль XML-парсов на нажатие;
- **S1**: `filterTouchesWhenObscured` на настройках и setup (tapjacking);
- **S2**: кап размера pending-файла личного словаря до чтения;
- **m1**: языково-верное сообщение отказа личного словаря (ru ≠ tt);
- **m2**: 200 tt-синонимов в поиске эмодзи (`emoji_search_v1.txt` изменился —
  осознанно не запинен; словари и биграммы байт-в-байт прежние, 16 пинов
  сверены).

Плюс tooling: в `scripts/emulator-smoke.sh` починен флаки `type-en-hi` на
release-APK — слепые тапы глобуса заменены функциональным зондом раскладки
(«ә»/«й»/«q» по точке (0.045, 0.665)) с двойным тапом переключения: зонд
печатает символ → commitText пересобирает цикл сабтипов
(`resetSubtypeCycleOrder`), поэтому одиночный тап после зонда всегда
возвращает предыдущую раскладку, а двойной идёт в третью.

## Гейты (HEAD)

| Гейт | Результат |
|---|---|
| `./gradlew test` | **1007 / 0 failures / 0 errors** (`--rerun-tasks`) |
| python-тесты (11 файлов) | **294 OK** (1 предсуществующий skip в emoji_pack) |
| `./gradlew lintRelease` | зелёный с baseline (31 warning зафиксирован) |
| `scripts/check-no-internet.sh` | оба уровня зелёные, включая `dist/tatar-keyboard-1.9.6.apk` |
| Воспроизводимость | два `clean assembleRelease` → одинаковый SHA-256 |
| `scripts/release_check.sh` | все проверки PASS |
| Смоук DEV-3 | tt_suggest_a14 release ×2: 15 PASS / 0 FAIL / 4 SKIP (by design) |

## Решения, принятые без оператора (обратимые)

1. **Версия 1.9.6 / versionCode 22** (по заданию миссии).
2. **Функциональный зонд в смоуке** вместо слепых тапов — поведение только
   release-ветки скрипта; debuggable-путь (run-as + преф) не изменён.
3. Пункт чек-листа про OpenSubtitles помечен решённым **по прямому решению
   оператора 2026-08-31** («забудь») — датированная пометка, история раздела
   не переписана.

## Что осталось открытым

* **Русская таблица биграмм: 4 195 голов из 10 000 разошлись со словарём** —
  следующая миссия; число записано в `docs/archive/bigrams/IMPERATIVE-HEADS.md`
  и KDoc `BigramArtifactSpec.RUSSIAN_BIGRAMS_V1`, мерить метрикой E5a.
* **Разговорный корпус** (`шалтырат` и повеления за рангом 15 000 молчат) —
  лечится корпусом, не отбором голов.
* **Замеры на железе**: PSS, холодный старт, эффект baseline-профиля,
  ландшафт (m3) и планшеты (m4) — ни разу не мерены на живом устройстве.
* **`tatar-keyboard-release.jks` лежит в корне репозитория** (не трекнут) —
  рекомендация вынести из каталога проекта в силе.
* **`git remote prune origin` отложен** — сети до origin не было; команда
  локально-безопасна, выполнить при появлении сети.
* **Публикация 1.9.6 — действие оператора:** push, тег наружу, релиз в стор
  (чек-лист `docs/PUBLISH-CHECKLIST.md`).
