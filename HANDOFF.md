# HANDOFF — миссия tt-layout-ergonomics

Обновлено: 2026-08-20. Ветка: `codex/version-1.4.0`.
Состояние миссии: **выполнена**.

Предыдущая миссия — `tt-symbol-edge`, её handoff вытеснен этим файлом; результаты
целиком в `docs/SYMBOL-KEY-EDGE-FIX.md`. Это была третья и последняя из трёх
клавиатурных миссий.

## Что просили

Полноценный ресерч по эргономике нижнего ряда, потом обоснованное решение, потом
реализация. Жалоба оператора: «кнопка эмодзи явно сильно укорачивает длину
пробела». Досье: `~/.supermanager/missions/tt-layout-ergonomics/dossier.md`.

## Что сделано

Полный отчёт — `docs/LAYOUT-ERGONOMICS.md` (заканчивается строкой `STATUS: done`),
скриншоты — `docs/layout-ergonomics/`.

Изменения вошли в коммит `84dba7c` (оператор закоммитил подготовленное дерево сам,
пока миссия дописывала отчёт; содержимое совпадает с тем, что было проверено).
Поверх него оператор своим коммитом `660c34f` поднял версию до 1.4.0 /
versionCode 6 — миссия файлов релиза не касалась.

### Главное из обмера

- **Досье указывало не на тот файл.** На телефоне для tt/ru/en применяется
  `key_space_5kw.xml` (через `row_qwerty4.xml`), а не `key_space_7kw.xml`.
  Реальная цена кнопки эмодзи — **10 %p**, а не 9 %p: пробел был 30 %p против
  40 %p.
- Пробел: **111.3dp** плитки (хитбокс 118.1dp) при глобусе + эмодзи.
- Настоящая проблема не в абсолютной ширине (18.7 мм — втрое выше любого
  рекомендованного минимума), а в **сдвиге**: левый край хитбокса пробела
  оказывался в **3.1 мм** от середины экрана, куда бьёт большой палец вслепую,
  при том что >70 % промахов ложатся в полосу 2 мм у границы клавиши. Промах
  влево открывал полноэкранную панель эмодзи.
- **Gboard на том же эмуляторе даёт пробелу 151.3dp и с одним языком, и с двумя.**
  Его нижний ряд не меняется вовсе: при появлении глобуса кнопка эмодзи не
  добавляется седьмой, а съезжает на долгое нажатие запятой (проверено руками).
  У AOSP LatinIME кнопки эмодзи в ряду нет вообще; у HeliBoard она есть, но и она,
  и глобус выключены по умолчанию.
- Средняя длина татарского слова по словарю проекта — 6.31 кодпоинта, то есть
  пробел это ~13.7 % всех нажатий.

### Решение

При включённом глобусе кнопка эмодзи из нижнего ряда убрана, панель эмодзи
переехала на **долгое нажатие запятой** (первая more-key, поэтому открывается без
скольжения) с подсказкой ☺ на клавише. Без глобуса кнопка эмодзи остаётся как
была. Правка целиком в XML: `key_space_5kw.xml`, `key_space_7kw.xml` и
`key_styles_settings.xml` (стиль запятой — так эмодзи не теряются и в режимах
url/email/dvorak).

| Проверка | Результат |
|---|---|
| `./gradlew test` | 723 теста, 0 failures / 0 errors / 0 skipped (было 719) |
| `./gradlew lintVitalRelease` | `BUILD SUCCESSFUL` |
| Release APK | 1 733 028 Б из 3 145 728 Б |
| Новые тесты красные до починки | да, 3 из 11 (проверено `git stash` на трёх XML) |
| Эмулятор | 4 комбинации флагов, tt/ru/en, портрет и альбом, долгое нажатие, тап по запятой, свайп по пробелу — таблица в отчёте, §7 |

Пробел **111.3dp → 150.5dp**, запас от середины экрана до чужой клавиши
**3.1 мм → 9.4 мм**.

## Что осталось незакрытым и почему

- **Планшет (sw600dp)** правку получил, но проверена она только тестом на XML:
  AVD с sw600dp в проекте нет, заводить его миссия не просила.
- **Живой палец.** `adb shell input` подаёт идеальный тап без дрожи, поэтому
  «стало меньше промахов» измерить нельзя — показано только, что запас до чужой
  клавиши вырос втрое. То же ограничение было у предыдущей миссии.
- `docs/TATAR-REVIEW-QUEUE.tsv` остался изменённым в рабочем дереве с миссии
  `tatar-version-bump`; этой миссией не трогался и не коммитился.
- Наружу ничего не публиковалось: ни push, ни tag, ни релиз.

## Требует решения оператора

Полный список — `docs/LAYOUT-ERGONOMICS.md`, §8. Коротко:

- **Долгое нажатие на запятую раньше отдавало paste, теперь эмодзи.** Paste никуда
  не делся (одно скольжение вправо), но это единственное изменение привычного
  жеста в правке.
- **Планшеты.** Если sw600dp важен, там можно вернуть отдельную кнопку эмодзи:
  45 %p на экране ≥600dp — не теснота. Цена — отдельный
  `res/xml-sw600dp/key_comma.xml`.
- **Обратный размен** — убрать из ряда запятую, а кнопку эмодзи оставить —
  рассмотрен и отвергнут по числам (запятая нажимается несравнимо чаще), но
  собирается за минуты, если оператор решит иначе.
- **Из миссии `tt-symbol-edge` всё ещё висит**: поднимать ли общий
  `keyHysteresisDistance` с 5dp до 8dp и трогать ли `mBogusMoveEventDetector`.
- Три клавиатурные миссии закрыты; версия уже поднята до 1.4.0 / versionCode 6
  коммитом `660c34f`. Дальше — пересборка и повторный аудит артефакта.
