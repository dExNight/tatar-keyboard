# HANDOFF — миссия tt-lang-priority

**Статус: закончена.** Отчёт `docs/LANG-PRIORITY.md` дописан и закрыт фразой-маркером последней
строкой — той, что ищет `done_when` досье. Больше эта фраза не встречается нигде в дереве.

Ветка `codex/version-1.6.0`. Наружу не уходило ничего: ни push, ни тега, ни релиза, ни merge.
Keystore `tatar-keyboard-release.jks` не трогался. Версия не поднималась, релиз не собирался.
Слов в словари не добавлялось, `approved` в `docs/DICTIONARY-*-REVIEW.tsv` не менялось.

## Что сделано

Закрыт `docs/RUSSIAN-BIGRAMS.md` §12 п. 8 — «нет правила приоритета кандидатов при конкуренции».

**Правило.** Полосой владеет язык текущей раскладки. Второй язык дописывается только в ячейки,
оставшиеся пустыми, и только с конца; первую ячейку он занимает лишь тогда, когда у текущего
языка нет ни одного кандидата. Второй движок спрашивают лениво (после неполного ответа своего)
и только если он уже тёплый. Автозамена, обучение личного словаря и наблюдение «пустой
результат» остаются целиком за текущим языком.

| Хеш | Что |
|---|---|
| `461513ef` | правило в `SuggestionsController` + 17 тестов, 13 из них красные до правки |
| `98adc824` | `LanguagePriorityCostTest` и тест работы на UI-потоке — измерение цены |
| последний | `docs/LANG-PRIORITY.md`, свидетельства и гарнессы в `docs/lang-priority/` |

База — `fbe8055d` (последний коммит прошлой миссии; лёг в дерево уже после старта этой).

## Главные числа

| Что | Значение |
|---|---|
| JVM-тесты | **934**, 0 падений, 0 ошибок, 1 skip, 92 suite-файла (база — 913 / 90) |
| Python-наборы | 156 пройдено, 1 skip, 102 subtest — как на базе |
| Обоснование правила | слияние по частоте увело бы первую ячейку чужому языку у **12,93 %** веса нажатий на татарской раскладке и **14,06 %** на русской; на первой букве — **38,1 %** и **29,1 %** |
| Шкалы частот несопоставимы | медиана отношения ru/tt 1,88 при p05 0,11 и p95 49,8, размах 0,000142…10 514 |
| Прибавка правила | чужой язык дописывает ячейку у 0,47 % (tt) и 0,81 % (ru) веса нажатий; для NEXT_WORD открывает молчавшую полосу у 0,27 % |
| Второй поиск | compute p95 **0,001 мс** (худший случай — однобуквенный префикс — 0,047), request→handoff p95 0,011 мс, бюджеты D1d 5 / 16 мс |
| Работа на UI-потоке | медиана 0,0004 мс, p95 0,0005 мс |
| Холодный старт (debug, AVD) | с правилом медиана 328,4 / худший 339,5; без правила 325,4 / 330,3; инвариант 400 |
| Эмулятор | 12 случаев на обеих раскладках, PREFIX и NEXT_WORD, совпали с офлайн-предсказанием все |

## Что осталось человеку

1. **Решение оператора: греть ли движок второго языка заранее.** Сейчас чужой язык дополняет
   полосу только после того, как человек хотя бы раз побывал на его раскладке в этом процессе
   IME. Сделать одинаково с первого нажатия можно, но это ~2,5 МБ распакованного словаря,
   отображение файла и рабочий поток за язык, которым человек может не пользоваться. Разбор —
   `docs/LANG-PRIORITY.md` §8 п. 1 и §10 п. 1.
2. Проверка на реальном телефоне.
3. `docs/RUSSIAN-BIGRAMS.md` §12 п. 8 можно закрывать.

## Что нашлось по дороге (не чинилось)

* Premise досье «слоты держат оба движка тёплыми» верна только после визита на обе раскладки в
  текущем процессе; в свежем процессе слот один.
* Debug-сборка на этом AVD отстаёт от набора на одно нажатие — воспроизведено **и без правки**
  (`fbe8055d`), на подписанном релизе 1.8.3 не воспроизводится. Правило тут ни при чём.
* Полоса — один Canvas-View, её ячеек нет в `uiautomator dump`; свидетельство только снимками.
  `screencap` на этом AVD работает и на сообщениях, и на клавиатуре.

## Незакрытых вопросов к оператору нет

Вопрос из пункта 1 «что осталось человеку» — предложение, а не блокировка: миссия закончена и
без него.
