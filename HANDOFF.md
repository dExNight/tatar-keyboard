# HANDOFF — миссия tt-version-1.7.0

**Статус: закончена.** `docs/APK-AUDIT-1.7.0.md` заканчивается маркером завершения.

## Что сделано

Собрана, подписана и проаудирована **версия 1.7.0 / versionCode 10** — 1.6.1 плюс русский
словарь и выбор словаря по раскладке. Работа велась в `codex/version-1.6.0`, frozen commit
**`b2e2f33`**. Наружу ничего не отправлялось: ни push, ни tag, ни Release, ни merge.

Полный отчёт со всеми числами — `docs/APK-AUDIT-1.7.0.md`. Скриншоты release-сборки —
`docs/apk-audit-1.7.0/`.

### Артефакт

`dist/tatar-keyboard-1.7.0.apk` — **2 386 361 Б**, SHA-256
`9e670053176907e410cf9528e92bb496a672f08f8d597db91812d3df41af3a62`, `cmp` с build output без
различий, `apksigner verify` → `Verifies`. После копирования пересборок не было.

### Evidence

* JVM suite — **798 тестов, 80 suite-файлов, 0 падений** (было 789/79), после `clean`.
* `lintVitalRelease` — BUILD SUCCESSFUL, задачи выполнялись, не из кэша.
* `check-no-internet.sh` — exit 0 на исходнике, debug и release APK; единственное
  разрешение — `VIBRATE` в обеих сборках.
* Версия подтверждена в `build.gradle`, обоих `output-metadata.json` и `aapt2 dump badging`.
* Подпись — v2 only, 1 signer, RSA 4096, `CN=Tatar Keyboard`, сертификат SHA-256
  `98ca6feb…` (тот же ключ от 2026-08-18, keystore не трогался).
* Размер 2 386 361 Б из 3 145 728 — запас 24,14 %.
* **Холодный старт замерен** (эмулятор `tatar_e5_test`, release-артефакт, `drop_caches`
  перед каждым прогоном): **289,5–316,8 мс, медиана 299,2** при инварианте 400 мс.
  A/B с переустановленной 1.6.1 на том же стенде — 265,0–327,1 мс, медиана 294,6.
  **Второй словарь старт не удлинил.**
* Функционально на **release**-сборке (R8 + shrinkResources): «слов» → Словам/Слова/Слово,
  «ещ» → Еще/Ещё/Ещенко, переключение глобусом обратно на татарскую → Беренче/Берсе/Берничә.
* `assets/dictionaries/NOTICE.txt` в APK, 2 077 Б, описывает оба словаря, CC BY 4.0.

### Документация

* `CHANGELOG.md` — раздел `[1.7.0]`; `[1.6.1]` и старше не тронуты.
* `metadata/en-US/changelogs/10.txt` — 432 Б, семь строк, построчно соответствуют разделу.
* `docs/PUBLISH-CHECKLIST.md` перецелен на v1.7.0 целиком; прежние разделы помечены как
  историческая запись. Разделы 3 и 5–7 не закрывались.

## Что осталось оператору (полный список — раздел «Что осталось сделать оператору» отчёта)

1. **Бэкап keystore вне этой машины** — седьмой прогон подряд невыполненным.
2. Проверка на телефоне: русские подсказки, «ё» долгим нажатием на «е», переключение
   раскладки туда-обратно, TalkBack.
3. Решение по целевой планке размера в `BRIEF.md` (2,2758 MiB против записанных 1,7 MiB;
   устно планка снята 2026-08-21, в файле не переписана — менять инвариант миссия не стала).
4. Судьба тегов 1.5.0 / 1.6.0 / 1.6.1; полный diff и release commit; push, CI, merge,
   tag `v1.7.0`, GitHub Release с SHA-256, заявка в IzzyOnDroid.

## Известные нерешённые вещи

* На реальном устройстве по-прежнему ни одного замера (холодный старт и PSS).
* TalkBack на этой сборке не проверялся — на эмуляторе он не установлен.
* «прив» не даёт «привет»: в русских корпусах Лейпцига нет разговорного регистра.
* Описание тумблера обещает предсказание следующего слова и для русского, а оно татарское.
* `E3a` — единственная `pending` в очереди вычитки, ждёт лично оператора.
