# HANDOFF — миссия tt-version-1.8.0

**Статус: закончена.** `docs/APK-AUDIT-1.8.0.md` заканчивается маркером завершения.

Ветка `codex/version-1.6.0`, frozen commit **`07b2c43`**. Наружу ничего не отправлялось:
ни push, ни tag, ни Release, ни merge, ни заявка в IzzyOnDroid. Keystore не трогался.

## Главное

**Русское предсказание следующего слова работает** — то, ради чего оператор просил эту
сборку. Проверено на подписанном release-артефакте (R8), а не по коду: `В связи ` →
**с / со / и**; татарское `Бер мин ` → **дә / үзем / бу** не изменилось.

Оговорка, которую надо сказать вслух: **корпуса новостные**. «привет», «давай», «ладно»,
«позвони» в таблицу не попали, и после них полоса будет пустой. Это измерено
(`docs/RUSSIAN-BIGRAMS.md`, раздел 7), прямо написано в changelog и вынесено оператору
как открытое решение, а не дефект.

## Артефакт

| | |
|---|---|
| Версия | 1.8.0 / versionCode 11 |
| Frozen commit | `07b2c4323b59874463b6bf69d67259218e60f1e6` |
| Release APK | 2 578 753 Б (запас до 3 145 728 — 18,02 %) |
| SHA-256 | `e7d99887b5316644ab8344e37916b312304850793324664f42755212371d1be5` |
| Копия | `dist/tatar-keyboard-1.8.0.apk`, `cmp` без различий, `apksigner verify` → `Verifies` |
| Сертификат | `98ca6feb…`, RSA 4096, `CN=Tatar Keyboard` — тот же ключ, что у 1.4.0…1.7.0 |

## Evidence

* **JVM suite — 809 тестов, 80 suite-файлов, 0 падений** (было 798/80), после `clean`
  с `--rerun-tasks`.
* **Python — 155 тестов в шести наборах `tests/`, 0 падений, 1 skip.**
* `lintVitalRelease` — `BUILD SUCCESSFUL`; 84 actionable tasks: 84 executed.
* no-INTERNET на исходнике и обоих APK, оба уровня плюс backup-whitelist; единственное
  разрешение — `VIBRATE` (отдельный `aapt2 dump permissions`).
* `output-metadata.json` и `aapt2 dump badging` — `versionCode 11`, `versionName 1.8.0`.
* **Холодный старт: 259,3–288,5 мс, медиана 278,9 (n=8)** при инварианте 400 — запас
  111,5 мс. A/B с переустановленной 1.7.0 в том же сеансе: 265,2–323,1, медиана 282,5.
  **Третья крупная таблица старт не удлинила.**
* Рост APK против 1.7.0 разобран по записям zip: +192 796 Б русская таблица, а
  `classes.dex` −219 Б и `resources.arsc` −656 Б — **уборка планшетного кода видна в
  артефакте**.
* Оба `NOTICE.txt` попали в APK и адресно соответствуют четырём лежащим в нём ассетам.

## Что сделано в дереве

| Файл | Что |
|---|---|
| `app/build.gradle` | `versionCode 11`, `versionName "1.8.0"` |
| `CHANGELOG.md` | новый раздел `[1.8.0]`; `[1.7.0]` и старше не тронуты |
| `metadata/en-US/changelogs/11.txt` | семь строк, 451 Б, построчно соответствует `[1.8.0]` |
| `docs/PUBLISH-CHECKLIST.md` | перецелен на v1.8.0 / versionCode 11 целиком |
| `docs/APK-AUDIT-1.8.0.md` | отчёт прогона |
| `docs/apk-audit-1.8.0/` | шесть скриншотов release-сборки |

**Кода эта миссия не меняла ни строки** — все правки закрыты предыдущими миссиями.

Отдельно: на старте дерево было грязным — отчёт `tt-small-fixes` и её `HANDOFF` остались
незакоммиченными. Хвост закрыт коммитом `a8c0d73`; сама миссия параллельно дописала свой
`HANDOFF` коммитом `5b29b43`. Истории никто не переписывал.

## Что закрылось в чеклисте

Раздел 2 — целиком (кроме намеренно открытого пункта про порванный upgrade path с v1.1.0),
раздел 4 — целиком, раздел 1 — частично. **Впервые закрыт пункт про очередь вычитки:**
`docs/TATAR-REVIEW-QUEUE.tsv` — 51 строка, все `approved`, проверено машинно.

Разделы 3 (device-UAT), 5, 6 и 7 не закрыты и закрываться этой миссией не могли.

## Что осталось оператору

Полный список — последний раздел `docs/APK-AUDIT-1.8.0.md`; кратко:

1. **Бэкап keystore вне этой машины** — девятый релизный прогон подряд невыполненным.
   Ключ в одном экземпляре; после публикации его потеря = обновить приложение нельзя
   никогда.
2. Проверка на телефоне: русское предсказание, переключение раскладки туда-обратно,
   татарское предсказание, поиск эмодзи с пробелами и с пустым запросом.
3. Решение по целевой планке размера в `BRIEF.md` (записано 1,7 MiB, фактически 2,46 MiB;
   запас сокращается четвёртый релиз подряд) и связанная с ним отсечка K=4.
4. Решение по разговорному корпусу для русских данных.
5. Просмотр полного diff, release commit, push, зелёный CI, merge, tag `v1.8.0`, GitHub
   Release с `dist/tatar-keyboard-1.8.0.apk` и его SHA-256, заявка в IzzyOnDroid.

## Состояние эмулятора

`tatar_e5_test` возвращён в исходное: локаль `en-US`, `adb unroot`, выбранная клавиатура —
`org.tatarkeyboard.ime`. **Намеренное отличие:** установлена 1.8.0, а не 1.7.0 — это
проаудированный артефакт из `dist/`. Откат при необходимости:
`adb install -r -d dist/tatar-keyboard-1.7.0.apk`.

**Грабли:** `assembleDebug` ставится отдельным пакетом `org.tatarkeyboard.ime.debug` и
**не** обновляет выбранную в системе релизную клавиатуру.
