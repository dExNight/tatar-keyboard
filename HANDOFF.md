# HANDOFF — миссия tt-version-1.8.3

**Статус: закончена.** Отчёт `docs/APK-AUDIT-1.8.3.md` дописан и закрыт фразой-маркером
последней строкой — той, что ищет `done_when` досье. Больше эта фраза не встречается нигде.

Ветка `codex/version-1.6.0`. Наружу не уходило ничего: ни push, ни тега, ни релиза, ни merge.
Keystore `tatar-keyboard-release.jks` не трогался и не пересоздавался. Слов в словари не
добавлялось, `approved` в `docs/DICTIONARY-*-REVIEW.tsv` не менялось.

## Что сделано

| Хеш | Что |
|---|---|
| `4ad8a83` | работа `tt-quarantine` (лежала в дереве незакоммиченной): код, тесты, строки, очередь вычитки |
| `00733b2` | `docs/QUARANTINE.md` |
| `bcc4391` | **frozen commit**: versionCode 14, versionName 1.8.3, `changelogs/14.txt`, раздел `[1.8.3]` в CHANGELOG |
| после него | `docs/APK-AUDIT-1.8.3.md`, ретаргет `docs/PUBLISH-CHECKLIST.md` на v1.8.3, один добавленный пункт в разделе `[1.8.3]` — ничего из этого в APK не попадает |

Релиз собран **из `bcc4391`**, дерево на момент сборки было чистым. Артефакт —
`dist/tatar-keyboard-1.8.3.apk`, SHA-256
`a5b1279357f0f8e0f03ba746ed88a750bcd3e369a23e1e5555ce6c2901fdb85c`, после копирования не
пересобирался.

## Главные числа

| Что | Значение |
|---|---|
| JVM-тесты | **913**, 0 падений, 0 ошибок, 1 skip (чужой `TapReproTest`), 90 suite-файлов |
| Python-наборы | **157**, 0 падений, 1 skip |
| `lintVitalRelease` | `No issues found.`, return-value `0` |
| release APK | **2 491 557 Б** (1.8.2 — 2 582 745, **−91 188**), запас 654 171 Б (20,80 %) |
| сертификат | `98ca6feb…42ad` — совпал с требуемым |
| разрешения | ровно `VIBRATE`; no-INTERNET gate exit 0 на исходнике и обоих APK |
| холодный старт | медиана 271,2 мс, худший 287,2 при инварианте 400 (n = 12 из 19 попыток) |
| обновление поверх 1.8.2 | таблицы переразвёрнуты за < 1 с, полоса подсказок **побайтно та же** 9 из 9, личный словарь цел |

## Что осталось человеку

Ничего обязательного для этой миссии. Оператору — раздел «Что осталось сделать оператору» в
`docs/APK-AUDIT-1.8.3.md`: бэкап keystore (одиннадцатый прогон подряд), проверка на телефоне,
целевая планка размера, просмотр полного diff, публикация (разделы 5–7 чеклиста), судьба
1.5.0…1.8.2.

Главная находка, вынесенная оператору: после обновления с 1.8.2 старые таблицы предсказания
с устройства не уходят (`MAX_FINAL_ARTIFACTS = 2`), поэтому APK убавил 91 188 Б, а устройство
прибавило 1 018 106. Не чинилось — это политика хранения, а не дефект.

## Незакрытых вопросов к оператору нет
