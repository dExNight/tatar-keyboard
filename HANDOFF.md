# HANDOFF — миссия tt-cleanup

**Статус: закончена.** Отчёт `docs/CLEANUP.md` заканчивается маркером `МЕЛКИЕ ДОЛГИ ЗАКРЫТЫ`,
который ищет `done_when`. Отчёт написан диспетчером после самостоятельной проверки: сессия
агента исчерпала окно контекста и не смогла его дописать, работа при этом была закончена.

Ветка `codex/version-1.6.0`. Наружу не уходило ничего: ни push, ни tag, ни релиз, ни merge.
Версия **не поднималась**, релиз **не собирался** — это делает `tt-version-1.8.3`. Keystore
`tatar-keyboard-release.jks` не трогался. Слов в словари не добавлялось, `approved` в
`docs/DICTIONARY-*-REVIEW.tsv` не менялось. Код карантина личного словаря не тронут —
он у следующей миссии `tt-quarantine`.

## Что сделано
1. Удалён мёртвый `app/src/main/res/values-sw430dp/config-per-form-factor.xml` (31 строка).
   Доказательства — четыре уровня, включая шесть конфигураций на эмуляторе; свидетельства
   в `docs/cleanup/*.png`, разбор в `docs/CLEANUP.md` § 1.
2. `values-sw600dp/config.xml` и `values-sw768dp/config.xml` **оставлены**: мёртвость не
   доказана, 26 из 26 имён читаются. Это решение, а не недоделка.
3. `docs/DICTIONARY-E5B.md` — две ссылки на исчезнувшую константу помечены † и снабжены
   датированной сноской; регулярное выражение теперь `BigramArtifactSpec.finalFilePattern`.
4. `BRIEF.md` и `PROPOSALS.md` — приведены в соответствие с решением оператора от
   2026-08-21: единственная действующая планка размера APK ≤ 3 МБ, целевая 1,7 МБ снята.

## Чем проверено
`./gradlew testDebugUnitTest` на финальном дереве, 2026-08-23: **87 классов, 860 тестов,
0 провалов, 1 пропуск** (прогнано дважды — агентом и диспетчером, числа совпали).

## Что дальше
`tt-quarantine` — карантин личного словаря. Дерево чистое, всё закоммичено локально.
Незакрытых вопросов к оператору нет.
