# HANDOFF — миссия tt-final

**Статус: закончена.** Отчёт `docs/FINAL-POLISH.md` дописан и закрыт фразой-маркером
последней строкой — той, что ищет `done_when` досье. Больше эта фраза не встречается нигде:
ни в `status.json`, ни в коммитах, ни выше по тексту отчёта.

Ветка `codex/version-1.6.0`. Наружу не уходило ничего: ни push, ни тега, ни релиза, ни merge.
Keystore `tatar-keyboard-release.jks` не трогался и не пересоздавался. Слов в словари не
добавлялось, `approved` в `docs/DICTIONARY-*-REVIEW.tsv` не менялось.

**Внимание следующему агенту:** в дереве лежит незакоммиченный `research/corpus/` — это работа
ПАРАЛЛЕЛЬНОЙ миссии (разговорный корпус), она качалась во время этой. Я её не трогал и не
коммитил. Не смешивайте её с этой версией: досье tt-final прямо запрещало подмешивать корпус.

## Что сделано

| Хеш | Что |
|---|---|
| `1837d964` | полоса: нарисованная подсказка всегда тапабельна; `@Ignore` миссии 1.8.1 снят |
| `a3ef5540` | четыре молчаливых отказа закрыты, сообщения на трёх языках |
| `b85d0661` | пропавший русский автокоррект, `ThreeLanguageStringsTest` |
| `2f9e5715` | **frozen commit**: versionCode 15, versionName 1.8.4, `changelogs/15.txt`, раздел `[1.8.4]` |
| `9529b3ab` | аудит, отчёт миссии, свидетельства, ретаргет чеклиста, очередь вычитки |

Релиз собран **из `2f9e5715`**, дерево на момент сборки было чистым. Артефакт —
`dist/tatar-keyboard-1.8.4.apk`, SHA-256
`9a7d57365c45ade644b935790ebebc70a0955acb9bc01f55b0ca1d0e29af74a8`, после копирования не
пересобирался (проверено `cmp` уже после коммита отчётов).

## Главные числа

| Что | Значение |
|---|---|
| JVM-тесты | **952**, 0 падений, 0 ошибок, **0 skip** (было 934 и 1 skip) |
| Python-наборы | 157, 0 падений, 1 skip |
| `lintVitalRelease` | return-value `0` |
| release APK | **2 496 781 Б** (1.8.3 — 2 491 557, **+5 224**), запас 648 947 Б (20,63 %) |
| из чего прирост | `assets/` **±0**, `res/` ±0, `resources.arsc` +4 324, `dex` +904 |
| сертификат | `98ca6feb…42ad` — совпал с требуемым |
| разрешения | ровно `VIBRATE`; no-INTERNET gate exit 0 на исходнике и обоих APK |
| холодный старт | медиана 266,5 мс, худший 281,9 при инварианте 400 (n = 15 из 20) |
| обновление поверх 1.8.3 | прошло, `firstInstallTime` не изменился, личный словарь цел |

## Что было главным

Мёртвые кнопки полосы — тот самый дефект, ради которого заведена цепочка, — **пойман на
подписанной 1.8.3, а не только в тесте**: поле «Вот кит», полоса «киеве / киев / кино», тап по
ячейке не меняет ничего (`docs/final-polish/dead-buttons-on-1.8.3.png`). На n = 15 таких
случаев было 4; на 1.8.4 их 0, вместо них 3 пустые полосы, и все три возвращаются со следующей
буквой. Доля верной полосы не изменилась (11 против 12).

Второе по важности: досье называло один случай долгого нажатия (по словарному слову), а
больший оказался рядом — при **выключенном** личном словаре жест молчал, и это состояние по
умолчанию, то есть у всех.

## Что осталось человеку

`docs/FINAL-POLISH.md`, раздел «Закрывает только человек с устройством в руках»: бэкап keystore
(двенадцатый прогон подряд), не мигает ли полоса на бюджетном телефоне (главный вопрос этой
версии), PSS, татарский интерфейс глазами (`tt-RU` эта система как локаль не принимает),
просмотр полного diff, публикация, судьба прежних артефактов.

## Незакрытых вопросов к оператору нет

`ask.json` не создавался: всё, что решалось, решалось само и записано в разделе «Решения,
принятые самостоятельно» отчёта.
