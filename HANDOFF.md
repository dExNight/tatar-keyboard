# HANDOFF — миссия tt-version-1.6.1

**Статус: закончена.** `docs/APK-AUDIT-1.6.1.md` заканчивается `STATUS: done`.

## Что сделано

Собрана, подписана и проаудирована версия **1.6.1 / versionCode 9** — 1.6.0 плюс две
починки поиска эмодзи из `tt-search-fixes`. Ветка та же, `codex/version-1.6.0`; новая под
патч не отводилась.

Frozen commit — **`652e1da`** (`chore(release): версия 1.6.1 / versionCode 9`): бамп
версии, раздел `[1.6.1]` в `CHANGELOG.md`, `metadata/en-US/changelogs/9.txt` (321 Б).
Разделы `[1.6.0]` и старше не переписывались.

Артефакт: `dist/tatar-keyboard-1.6.1.apk`, **1 801 038 Б**, SHA-256
`c48246ab4301848d71c02c116c3e235708e47a5f75013c67d8e914fb60d0fac5`, `cmp` с build output
без различий. Сборка **не байт-в-байт воспроизводима** — пересборка того же коммита даёт
другой SHA-256, поэтому в Release вкладывать именно этот файл.

Evidence прогона (все числа сняты на этом артефакте, ничего не перенесено):

- JVM suite — 789 тестов, 0/0/0, 79 suite-файлов (было 780/78);
- `lintVitalRelease` — `BUILD SUCCESSFUL`;
- `check-no-internet.sh` — exit 0 на debug и release, оба уровня плюс backup-whitelist;
- permission dump — только `VIBRATE` в обоих APK;
- размер 1 801 038 Б из 3 145 728 (запас 42,75 %), на 452 Б меньше 1.6.0;
- подпись — v2 only, один signer, RSA 4096, `CN=Tatar Keyboard`, cert SHA-256
  `98ca6feb…` (ключ не менялся);
- `output-metadata.json` — `versionCode 9` / `1.6.1` в release и debug.

`docs/PUBLISH-CHECKLIST.md` перецелен на v1.6.1 / 9 целиком; разделы 3 и 5–7 не
закрывались. Очередь вычитки: 43 `approved` + 1 `pending` (словарная `E3a`, ждёт лично
оператора).

## Два отклонения, оба унаследованные

1. Целевой бюджет D1 ≤ 1,7 MiB превышен — 1,7176 MiB, перебор 18 459 Б (в 1.6.0 было
   18 911). Обязательный потолок `BRIEF.md` 3 МБ выполняется.
2. Подпись не совпадает с историческим сертификатом v1.1.0 — upgrade path порван решением
   оператора от 2026-08-18.

## Главный риск

**TalkBack на этой сборке не проверял никто** — на эмуляторе `tatar_e5_test` он не
установлен. Дыра шире, чем в 1.5.0: попап кожных тонов из 1.6.0 забирает всё дерево
виртуальных узлов, а в 1.6.1 менялась геометрия поля поиска. Закрыто только юнит-тестами.

## Дальше

Работы в рамках миссии не осталось. Оператору — список из восьми пунктов в конце
`docs/APK-AUDIT-1.6.1.md`; первым бэкап keystore, вторым проверка двух починенных мест на
телефоне, третьим развилка «выпускались ли Release 1.5.0 и 1.6.0»
(`git ls-remote --tags origin`).

Наружу ничего не публиковалось, remote не опрашивался. `dist/tatar-keyboard-1.6.0.apk` и
`docs/APK-AUDIT-1.6.0.md` не тронуты.
