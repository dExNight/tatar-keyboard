• Fixed: the suggestion strip no longer stays empty after the cursor moves
• A tap in the text, a space slide or a delete swipe used to blank it until the next key press
• The strip now comes back on its own, with the words for the word the cursor stopped at
• It still stays empty where it should: cursor inside a word, emoji panel, emoji search
• Dictionaries, next-word tables, layouts and permissions are byte-for-byte unchanged
