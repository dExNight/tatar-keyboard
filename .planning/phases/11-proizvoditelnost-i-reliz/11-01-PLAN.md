---
phase: 11-proizvoditelnost-i-reliz
plan: 01
slug: proizvoditelnost-i-reliz
type: execute
wave: 1
depends_on: []
files_modified:
  - app/build.gradle
  - app/src/main/res/raw/keep.xml
  - app/src/main/res/values/strings-appname.xml
  - app/src/main/res/values/strings.xml
  - .github/workflows/ci.yml
  - README.md
  - PRIVACY.md
  - CHANGELOG.md
  - docs/PUBLISH-CHECKLIST.md
  - .planning/phases/11-proizvoditelnost-i-reliz/11-PERF-CHECKLIST.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: [PERF-01, PERF-02, PERF-03, REL-01, REL-02, REL-03]

must_haves:
  truths:
    - "Release APK ≤ 3 МБ — stat-гейт подтверждён числом (PERF-01 механически)"
    - "locale_name_tt_RU не стрипнута shrinkResources: keep.xml с @string/locale_name_* предотвращает Resources.NotFoundException в LocaleResourceUtils — краш спейсбара «Татарча» (R1 из CONTEXT.md, снят живым экспериментом RESEARCH.md)"
    - "Имя приложения отображается «Tatar Keyboard» без суффикса разработки во всех 4 label манифеста (единая точка @string/english_ime_name — RESEARCH.md §3)"
    - "Подписанный release-APK верифицируется apksigner verify (REL-01 — ключ уже существует, задача только гейт)"
    - "CI падает при release APK > 3 МБ: stat-гейт в ci.yml (PERF-01 как CI-контракт)"
    - "Privacy policy содержит утверждение об отсутствии сбора данных и ссылку на лицензию; README её линкует (REL-02)"
    - "PUBLISH-CHECKLIST.md содержит точные команды и URL для ручной публикации GitHub Release + заявки IzzyOnDroid (REL-03 — подготовлено, публикация деферрирована)"
    - "PERF-02/03 деферрированы: 11-PERF-CHECKLIST.md содержит конкретные adb-команды с порогами для device-сессии"
  artifacts:
    - path: app/src/main/res/raw/keep.xml
      provides: "Resource shrinker keep-rules: locale_name_*, label_*, kbd_*, keyboard_layout_set_*, row* — все 4 getIdentifier call-site защищены"
    - path: app/build.gradle
      provides: "shrinkResources true (buildType release) + versionName 1.0.0 + versionCode 1"
    - path: .github/workflows/ci.yml
      provides: "assembleRelease (unsigned, без keystore.properties) + stat size-gate ≤ 3145728"
    - path: README.md
      provides: "Описание проекта, фичи, установка APK, приватность, ссылки PRIVACY.md + PUBLISH-CHECKLIST.md"
    - path: PRIVACY.md
      provides: "Privacy policy: данные не собираются, без INTERNET, Apache-2.0"
    - path: CHANGELOG.md
      provides: "v1.0.0 release notes по 11 фазам → user-facing features"
    - path: docs/PUBLISH-CHECKLIST.md
      provides: "Пошаговая инструкция: GitHub repo, push, tag v1.0.0, GitHub Release с APK, IzzyOnDroid — точные команды/URL, ручное исполнение"
    - path: .planning/phases/11-proizvoditelnost-i-reliz/11-PERF-CHECKLIST.md
      provides: "adb-команды PERF-02/03 с точными порогами: dumpsys meminfo PSS ≤ 30 МБ, am start -W < 400 мс, Profiler GC=0, gfxinfo janky=0"
  key_links:
    - "shrinkResources→keep.xml→locale_name_tt_RU: отсутствие keep.xml ≡ краш спейсбара «Татарча» (Resources.NotFoundException в LocaleResourceUtils.initLocked)"
    - "versionName «1.0.0» в build.gradle → запись в CHANGELOG.md → тег v1.0.0 в PUBLISH-CHECKLIST.md: тройное согласование"
    - "keystore.properties → signingConfigs.release → assembleRelease → apksigner verify OK (REL-01)"
---

# Plan 11-01 — Финальный релиз: shrinkResources + keep, CI-гейт, документы, верификация (wave 1)

**Goal (ROADMAP SC):** Замеренные бюджеты производительности + подписанный release + публикация — граница майлстоуна v1.0.

## Phase Goal

**As a** татароязычный пользователь на бюджетном Android, **I want to** получить стабильный v1.0 APK ≤ 3 МБ с татарской раскладкой, **so that** я могу установить его с GitHub Releases и печатать по-татарски так же быстро, как по-русски в Gboard, — без интернета и слежки.

**Контекст фазы:** Все 10 предыдущих фаз structure-verified. Фаза 11 — финальные правки build-конфига + CI-гейт + документы + верификация. Код не пишется: нет новых features, нет новой логики. По RESEARCH.md: keystore уже готов, minifyEnabled уже true, подписанный APK уже существует. Главный содержательный риск (R1 shrinkResources vs getIdentifier) снят живым экспериментом — решение конкретное и проверенное.

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

- Источник правды: `11-RESEARCH.md` (все вердикты по живым сборкам + дампам, рабочее дерево откачено) + `11-CONTEXT.md` (locked decisions).
- **Locked decisions (не пересматривать):** minSdk 24 (Android 7.0); applicationId `org.tatarkeyboard.ime`; публикация — только вручную; GitHub-репо не создано.
- База boundary-диффа: **7f99505** (текущий HEAD после docs-коммита ресерча).
- **keep.xml содержимое (из живого эксперимента RESEARCH.md §1):** инструменты к executor — `<?xml version="1.0" encoding="utf-8"?><resources xmlns:tools="http://schemas.android.com/tools" tools:keep="@xml/keyboard_layout_set_*,@xml/kbd_*,@xml/rows_*,@xml/rowkeys_*,@xml/row_*,@string/locale_name_*,@string/label_*" />` — использовать точно этот список масок; xml-маски избыточны (AGP safe mode уже сохраняет раскладки), но оставлены как страховка от смены поведения AGP.
- **Прецедент формата:** 10-01-PLAN.md (структура задач, standing-паттерн деферрала UAT, boundary-диф, bookkeeping).

<threat_model>
**ASVS Level 1 · block_on: high**

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Сборочный pipeline → release keystore | keystore.properties + release.jks — секреты только локально, не в CI |
| CI (GitHub Actions) → собранный APK | unsigned APK в CI — намеренно; секреты в CI не нужны и не должны быть |
| Пользователь → APK (установка с GitHub Releases) | APK подписан известным ключом; IzzyOnDroid дополнительно верифицирует reproducibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | release.jks / keystore.properties | critical | mitigate | Оба файла gitignored (проверено фазой 1); PUBLISH-CHECKLIST содержит напоминание о бэкапе jks вне репо; CI работает без секретов (unsigned APK) |
| T-11-02 | Information Disclosure | shrinkResources стрипает locale_name_tt_RU | high | mitigate | keep.xml с @string/locale_name_* (проверено экспериментом); post-build гейт aapt2 ≥ 8 locale_name вхождений |
| T-11-03 | Spoofing | README/PRIVACY содержат ложные утверждения о приватности | medium | mitigate | PRIVACY.md утверждает «без INTERNET» — это структурно верифицируемо (PERF-04 CI + check-no-internet.sh); никаких аналитик/Firebase в deps |
| T-11-04 | Denial of Service | CI-гейт неправильно считает APK size и ложно падает | low | mitigate | Используется `stat -c%s` (байты, не человекочитаемый вывод); числовое сравнение `test ... -le 3145728`; debug-APK 2.38 МБ тоже проходит как запасной вариант |
| T-11-SC | Tampering | npm/pip/cargo installs | low | accept | Фаза не устанавливает пакетов вообще; только build.gradle — ноль новых зависимостей; Package Legitimacy Audit = Not applicable |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: release-конфиг — shrinkResources + keep.xml + ребрендинг + versionName (PERF-01, REL-01)</name>
  <read_first>
    - app/build.gradle — текущий state: versionName "0.1.0" (:16), shrinkResources отсутствует (:32-38), signingConfigs.release условный (:18-27)
    - app/src/main/res/values/strings-appname.xml — english_ime_name "Tatar Keyboard (dev)" (:22); privacy_policy_url/license_url placeholders (.invalid, :27-28)
    - .planning/phases/11-proizvoditelnost-i-reliz/11-RESEARCH.md — §1 keep.xml точное содержимое + §3 ребрендинг + §6 PERF-01 факт
  </read_first>
  <files>app/build.gradle, app/src/main/res/raw/keep.xml, app/src/main/res/values/strings-appname.xml, app/src/main/res/values/strings.xml</files>
  <action>
    Реализует locked decisions CONTEXT + RESEARCH.md §1/3/6. Ровно 4 файловых изменения:

    1. **app/build.gradle** — в buildTypes.release добавить строку `shrinkResources true` (сразу после `minifyEnabled true`, :33); поднять `versionName` с "0.1.0" до "1.0.0" (:16). versionCode оставить 1 (первый публичный релиз — code совпадает). Ноль новых зависимостей, namespace/applicationId/compileSdk/minSdk — не трогать. Итог buildTypes.release: `minifyEnabled true`, `shrinkResources true`, proguard-lines, signingConfig-условие.

    2. **НОВЫЙ app/src/main/res/raw/keep.xml** — создать директорию `res/raw/` и файл. Содержимое: XML-декларация + `<resources xmlns:tools="http://schemas.android.com/tools" tools:keep="@xml/keyboard_layout_set_*,@xml/kbd_*,@xml/rows_*,@xml/rowkeys_*,@xml/row_*,@string/locale_name_*,@string/label_*" />`. Маски покрывают все 4 getIdentifier call-site (RESEARCH.md §1, таблица): call-site 1 (keyboard_layout_set_*+kbd_*+rows_*+rowkeys_*+row_*), call-site 2+3 (locale_name_*), call-site 4 (label_*). Xml-маски избыточны при AGP safe mode, но страхуют от смены поведения — оставить.

    3. **app/src/main/res/values/strings-appname.xml** — два точечных изменения:
       - `english_ime_name`: убрать суффикс разработки → итоговое значение `Tatar Keyboard`. Только это значение, все 4 label манифеста обновятся автоматически через единую точку (RESEARCH.md §3 — манифест не трогать).
       - `privacy_policy_url` + `license_url`: НЕ трогать в этом таске — owner GitHub-репо неизвестен (репо не создано, аккаунт пользователя нам недоступен). Замена `.invalid` → реальные URL — явный шаг в PUBLISH-CHECKLIST.md (Task 3): после создания репо пользователь подставляет свой owner, пересобирает и перепроверяет APK перед тегом v1.0.0. `english_ime_settings` не трогать — используется как key в prefs.xml, не как видимый label.

    4. **app/src/main/res/values/strings.xml** — удалить элемент с именем `setup_message` (строка 126). Это единственная мёртвая строка с упоминанием старого бренда в базовом файле values/ (0 вхождений в java/kt по RESEARCH.md §3 + удалённый legacy-диалог фазы 10). Удалить ровно эту одну запись. Остальные ~30 локализованных копий в values-*/strings.xml НЕ трогать (lint ворчит на orphan, но не падает; полный ребрендинг 35 файлов = backlog post-v1.0 по CONTEXT.md § Deferred).

    Особенности: `res/raw/` не существует — создать директорию. Граница минимального диффа: ровно эти 4 файла, ноль других.
  </action>
  <verify>
    <automated>cd /Users/amirka/workspace/main/development/tatar-keyboard && ./gradlew assembleRelease && APK=$(ls app/build/outputs/apk/release/*.apk 2>/dev/null | head -1) && test -n "$APK" && APKSIZE=$(stat -c%s "$APK" 2>/dev/null || stat -f%z "$APK") && test "$APKSIZE" -le 3145728 && echo "APK size OK: $APKSIZE bytes" && bash scripts/check-no-internet.sh "$APK" && aapt2 dump resources "$APK" | grep -c 'locale_name' | xargs -I{} test {} -ge 8 && aapt2 dump resources "$APK" | grep -q 'keyboard_layout_set_tatar' && test $(aapt2 dump resources "$APK" | grep -cE 'label_pause_key|label_wait_key') -ge 2 && test -f app/src/main/res/raw/keep.xml && grep -q 'locale_name_\*' app/src/main/res/raw/keep.xml && grep -q 'shrinkResources true' app/build.gradle && grep -q '"1.0.0"' app/build.gradle && grep -q '>Tatar Keyboard<' app/src/main/res/values/strings-appname.xml && ! grep -q '(dev)' app/src/main/res/values/strings-appname.xml && ! grep -q 'name="setup_message"' app/src/main/res/values/strings.xml && { apksigner verify "$APK" 2>/dev/null || $ANDROID_HOME/build-tools/*/apksigner verify "$APK"; }</automated>
  </verify>
  <done>assembleRelease зелёный; release APK ≤ 3 145 728 байт; apksigner verify OK; aapt2 dump release APK: locale_name ≥ 8 вхождений (locale_name_tt_RU не стрипнута), keyboard_layout_set_tatar присутствует, label_pause_key/label_wait_key = 2; english_ime_name = "Tatar Keyboard" без «(dev)»; setup_message удалён из values/strings.xml; shrinkResources true в build.gradle; versionName "1.0.0"; privacy_policy_url/license_url остаются .invalid-плейсхолдерами намеренно (замена — шаг PUBLISH-CHECKLIST при известном owner репо).</done>
</task>

<task type="auto">
  <name>Task 2: CI — assembleRelease (unsigned) + APK size gate (PERF-01 как CI-контракт)</name>
  <read_first>
    - .github/workflows/ci.yml — полный файл (45 строк): текущая структура, шаги, формат indent
    - .planning/phases/11-proizvoditelnost-i-reliz/11-RESEARCH.md — §5 CI: что добавить, почему unsigned OK, почему дописывать в существующий workflow а не создавать новый
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    Дописать в существующий `.github/workflows/ci.yml` (НЕ создавать новый workflow — RESEARCH.md §5) два шага ПОСЛЕ шага "Build debug APK" и ПЕРЕД шагом "Check no INTERNET permission (built APK)":

    1. **Шаг assembleRelease (unsigned):** `name: Build release APK (unsigned)` / `run: ./gradlew assembleRelease`. Без keystore.properties (она gitignored, в CI её нет) — по design build.gradle(:35-37) условно пропускает signingConfig; собирается unsigned APK. Это корректно для CI: цель — проверить размер и ресурсы, не подпись (подпись верифицируется локально apksigner).

    2. **Шаг size gate (PERF-01):** `name: Check release APK size (PERF-01 ≤ 3 MB)` / `run:` однострочник:
       ```
       test $(stat -c%s app/build/outputs/apk/release/app-release-unsigned.apk) -le 3145728
       ```
       `test` возвращает non-zero при превышении → шаг падает → CI красный (fail-capable). Имя файла `app-release-unsigned.apk` — AGP-конвенция для release без signingConfig. Бюджет: 3 145 728 байт = ровно 3 МБ (1024×1024×3). Текущий размер 681 078 байт (с shrink) — запас 4.6× (RESEARCH.md §6).

    Порядок шагов после правки: check-no-internet (fast-fail) → setup-java → setup-gradle → restore-jar → assembleDebug → assembleRelease (unsigned) → size-gate → check-no-internet (built APK) → upload-artifact.

    Ресурсный гейт aapt2 (locale_name ≥ 8) в CI пока НЕ добавлять: требует aapt2 на PATH в ubuntu-latest без явной установки build-tools, что добавило бы ~2 строки setup — сложность не оправдана в CI при наличии local-гейта в Task 4. Если в будущем нужен — отдельный PR.

    Ноль изменений в других файлах workflow. Indent — 2 пробела (конвенция существующего файла).
  </action>
  <verify>
    <automated>cd /Users/amirka/workspace/main/development/tatar-keyboard && grep -n 'Build release APK' .github/workflows/ci.yml && grep -n 'assembleRelease' .github/workflows/ci.yml && grep -n 'PERF-01' .github/workflows/ci.yml && grep -n '3145728' .github/workflows/ci.yml && grep -n 'stat -c%s' .github/workflows/ci.yml && { python3 -c "import yaml" 2>/dev/null && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo "YAML valid" || echo "pyyaml unavailable — structural greps are the gate"; }</automated>
  </verify>
  <done>ci.yml содержит шаги assembleRelease (unsigned) и size-gate с `stat -c%s ... -le 3145728`; YAML валиден (python yaml.safe_load OK); оба шага присутствуют после assembleDebug; ноль других изменений в workflow.</done>
</task>

<task type="auto">
  <name>Task 3: Документы — README, PRIVACY, CHANGELOG, PUBLISH-CHECKLIST, PERF-CHECKLIST (REL-02, REL-03 подготовка)</name>
  <read_first>
    - README.md — текущий (наследие Simple Keyboard: чужие бейджи F-Droid/Play, чужие ссылки — заменить целиком)
    - PRIVACY.md — текущий (1 строка — расширить)
    - .planning/phases/11-proizvoditelnost-i-reliz/11-RESEARCH.md — §4 keystore-вердикт (бэкап-напоминание) + §6 PERF-таблица + §8 форма Task 3
    - .planning/ROADMAP.md — 11 фаз с goal-строками (сырьё для CHANGELOG)
    - .planning/phases/11-proizvoditelnost-i-reliz/11-CONTEXT.md — § Документы (состав каждого файла)
  </read_first>
  <files>README.md, PRIVACY.md, CHANGELOG.md, docs/PUBLISH-CHECKLIST.md, .planning/phases/11-proizvoditelnost-i-reliz/11-PERF-CHECKLIST.md</files>
  <action>
    Пять документов. Язык: README/PRIVACY/CHANGELOG — двуязычно не нужно, русский основной + краткая английская шапка в README (аудитория — татароязычные пользователи в РФ; IzzyOnDroid ревьюеры читают английский — компромисс: README имеет короткий английский абзац-lead, остальное по-русски); PUBLISH-CHECKLIST/PERF-CHECKLIST — русский (внутренние документы соло-разработчика).

    1. **README.md — переписать целиком** (текущий — README Simple Keyboard с чужими бейджами и ссылками, оставлять нельзя):
       - Шапка: «Tatar Keyboard» + один абзац по-английски (native Android IME, Tatar Cyrillic layout with dedicated fifth row ә ө ү җ ң һ, fully offline, no INTERNET permission, Apache-2.0).
       - По-русски: описание (стандартная ЙЦУКЕН + видимый пятый ряд — дифференциатор, которого нет у Gboard/SwiftKey), список фич по фазам-возможностям: 3 раскладки (tt_RU/ru/en_US) + глобус, long-press дубли татарских букв на русской раскладке, iOS-стиль (светлая/тёмная тема), double-space→точка, свайп-курсор по пробелу, multi-touch, TalkBack-доступность с татарскими описаниями букв, онбординг, directBoot.
       - Установка: скачать APK из GitHub Releases → разрешить установку из неизвестных источников → онбординг проведёт (2 шага). Требования: Android 7.0+ (minSdk 24).
       - Приватность: «данные не собираются — в манифесте нет разрешения INTERNET, это проверяется CI на каждом коммите»; ссылка на [PRIVACY.md](PRIVACY.md).
       - Сборка из исходников: `./gradlew assembleDebug` (кратко, 3-4 строки: JDK 17, восстановление gradle-wrapper.jar — упомянуть scripts или README-инструкцию из 01-02).
       - Лицензия: Apache-2.0, форк [Simple Keyboard](https://github.com/rkkr/simple-keyboard) (attribution обязателен).
       - Для мейнтейнера: ссылка на [docs/PUBLISH-CHECKLIST.md](docs/PUBLISH-CHECKLIST.md).
       - **Запреты:** НЕ вставлять скриншот-плейсхолдеры («скриншоты будут» — хуже отсутствия секции); НЕ вставлять бейджи неактивных сервисов (F-Droid/IzzyOnDroid/CI — станут активны после публикации, добавить тогда; мёртвый бейдж = красный флаг для ревьюеров IzzyOnDroid); НЕ упоминать iPhone/iOS в маркетинговом контексте (legal-граница CLAUDE.md — «в стиле iOS» не писать, писать «знакомая эргономика»).
    2. **PRIVACY.md — расширить до полноценной policy** (сейчас 1 строка): заголовок «Политика конфиденциальности / Privacy Policy» (двуязычно — короткий документ, EN-блок для IzzyOnDroid), содержание: приложение не собирает, не хранит и не передаёт никакие данные; разрешения INTERNET нет в манифесте (проверяемо: `aapt2 dump permissions` + CI-гейт на каждом коммите); клавиатура работает полностью офлайн; нет аналитики, нет рекламных SDK, нет Firebase; вводимый текст обрабатывается только на устройстве и никуда не отправляется; исходный код открыт (Apache-2.0). Дата версии policy. Никаких email/контактов не выдумывать — контакт = GitHub Issues (URL добавится после публикации, написать «Issues репозитория»).
    3. **CHANGELOG.md — создать**, формат Keep a Changelog упрощённо: `## [1.0.0] — 2026-07-19` (дату поставить фактическую на момент исполнения), user-facing список (НЕ по фазам-номерам, а по фичам — пользователь фаз не знает): татарская раскладка ЙЦУКЕН + пятый ряд ә ө ү җ ң һ; русская и английская раскладки, переключение глобусом; long-press татарские буквы на русской раскладке (а→ә...); слои ?123/#+=; shift/caps-lock, автокапитализация, ускоряющийся backspace, Enter по действию поля; двойной пробел→точка, свайп-курсор, multi-touch; светлая/тёмная тема в стиле знакомой эргономики; мгновенный баллон-превью, панель альтернатив; звук/вибрация с настройками; TalkBack с татарскими описаниями («татарская э» для ә); онбординг из 2 шагов; работа до первой разблокировки (directBoot); полностью офлайн, без разрешения INTERNET, APK < 1 МБ.
    4. **docs/PUBLISH-CHECKLIST.md — создать** (директории docs/ нет — создать). Пошаговая инструкция ДЛЯ РУЧНОГО выполнения создателем (executor НИЧЕГО из неё не исполняет — prohibition). Шаги с точными командами:
       1. Бэкап `release.jks` + `keystore.properties` минимум в 2 места вне репо (пароль-менеджер + офлайн-носитель); **потеря ключа = невозможность обновлений** (standing blocker фазы 1).
       2. Создать GitHub-репо `tatar-keyboard` (public, БЕЗ auto-README — история уже есть): `https://github.com/new`.
       3. Подставить реальный owner в `privacy_policy_url`/`license_url` (`app/src/main/res/values/strings-appname.xml` — заменить `.invalid`-плейсхолдеры), закоммитить.
       4. `git remote add origin git@github.com:<owner>/tatar-keyboard.git && git push -u origin main`.
       5. Дождаться зелёного CI на Actions; прогнать негативный тест PERF-04 (ветка с INTERNET-разрешением → CI красный → ветку удалить) — доказательство работы гейта (deferred blocker фазы 1).
       6. Пересобрать release локально (`./gradlew clean assembleRelease`), проверить `apksigner verify`, размер ≤ 3 МБ.
       7. Тег: `git tag v1.0.0 && git push origin v1.0.0`.
       8. GitHub Release: `https://github.com/<owner>/tatar-keyboard/releases/new` → тег v1.0.0 → заголовок «Tatar Keyboard 1.0.0» → тело из CHANGELOG.md → приложить `app-release.apk` (подписанный, переименовать `tatar-keyboard-1.0.0.apk`).
       9. Заявка IzzyOnDroid: `https://gitlab.com/IzzyOnDroid/repo/-/issues/new` (шаблон «App inclusion request») — указать repo-URL; требования Izzy: подписанный APK в Releases, лицензия в репо, privacy policy — всё готово к этому шагу.
       10. После включения в Izzy — добавить бейдж в README (отдельный коммит).
    5. **.planning/phases/11-proizvoditelnost-i-reliz/11-PERF-CHECKLIST.md — создать**: чек-лист device-сессии PERF-02/03 с точными командами и порогами (сырьё для Task 5 / UAT-бандла):
       - Подготовка: release-APK (не debug — minify влияет), бюджетное устройство (Xiaomi/Redmi или Samsung A), клавиатура выбрана дефолтной.
       - **PERF-02a PSS ≤ 30 МБ:** открыть клавиатуру в Telegram → `adb shell dumpsys meminfo org.tatarkeyboard.ime` → строка `TOTAL PSS` ≤ 30720 KB; замер ×3 (после ротации, после смены раскладки tt→ru→en) — все ≤ 30 МБ.
       - **PERF-02b холодный старт < 400 мс:** `adb shell am force-stop org.tatarkeyboard.ime` → тап в текстовое поле, метрика — от тапа до показа клавиатуры; инструмент: `adb logcat -c && adb logcat -v time | grep -iE 'latinime|inputmethod'` (окно от bindInput до onWindowShown) ЛИБО скринрекорд 60fps с покадровым счётом (моторика: 24 кадра @60fps = 400 мс); ×5 прогонов, медиана < 400 мс.
       - **PERF-03a ноль аллокаций:** Android Studio Profiler → Memory → запись аллокаций → 30 с непрерывной печати → фильтр по `rkr.simplekeyboard` — 0 аллокаций из onDraw/onTouchEvent-стеков; 0 GC-событий за сессию печати (Profiler timeline).
       - **PERF-03b janky ~0%:** `adb shell dumpsys gfxinfo org.tatarkeyboard.ime reset` → 60 с активной печати → `adb shell dumpsys gfxinfo org.tatarkeyboard.ime` → `Janky frames: X (Y%)` — Y ≤ 1% (порог «~0%» ROADMAP; 0.x% от системных событий допустимо).
       - Каждый пункт — с местом для записи фактического числа (таблица «замер → факт → PASS/FAIL»).
  </action>
  <verify>
    <automated>cd /Users/amirka/workspace/main/development/tatar-keyboard && test -f README.md && test -f PRIVACY.md && test -f CHANGELOG.md && test -f docs/PUBLISH-CHECKLIST.md && test -f .planning/phases/11-proizvoditelnost-i-reliz/11-PERF-CHECKLIST.md && grep -q 'PRIVACY.md' README.md && grep -q 'PUBLISH-CHECKLIST' README.md && grep -qi 'apache' README.md && grep -qi 'simple keyboard' README.md && ! grep -qiE 'скриншот.*(скоро|будет|placeholder)|screenshot.*(soon|placeholder|TBD)' README.md && ! grep -qE '!\[.*badge.*\]|img.shields.io' README.md && ! grep -qiE 'iphone|(в стиле|как в|like) iOS' README.md && grep -qi 'INTERNET' PRIVACY.md && grep -qiE 'не собира|not collect' PRIVACY.md && grep -qi 'apache' PRIVACY.md && grep -q '1.0.0' CHANGELOG.md && grep -q 'ә ө ү җ ң һ' CHANGELOG.md && grep -qi 'talkback' CHANGELOG.md && grep -q 'release.jks' docs/PUBLISH-CHECKLIST.md && grep -qi 'бэкап\|backup' docs/PUBLISH-CHECKLIST.md && grep -q 'github.com/new' docs/PUBLISH-CHECKLIST.md && grep -q 'v1.0.0' docs/PUBLISH-CHECKLIST.md && grep -qi 'izzyondroid' docs/PUBLISH-CHECKLIST.md && grep -q 'gitlab.com/IzzyOnDroid' docs/PUBLISH-CHECKLIST.md && grep -q 'strings-appname' docs/PUBLISH-CHECKLIST.md && P=.planning/phases/11-proizvoditelnost-i-reliz/11-PERF-CHECKLIST.md && grep -q 'dumpsys meminfo' $P && grep -q '30720\|30 МБ' $P && grep -q '400' $P && grep -q 'force-stop' $P && grep -qi 'profiler' $P && grep -q 'gfxinfo' $P && grep -qi 'janky' $P</automated>
  </verify>
  <done>5 документов существуют и содержательны: README (без placeholder-скриншотов, без мёртвых бейджей, без iPhone/iOS-маркетинга, с attribution Simple Keyboard + ссылками PRIVACY/PUBLISH-CHECKLIST), PRIVACY («данные не собираются» + no-INTERNET + Apache), CHANGELOG (v1.0.0, фичи user-facing, пятый ряд буквами), PUBLISH-CHECKLIST (бэкап jks → repo → URL-фикс strings-appname → push → CI+негативный тест → тег → Release → IzzyOnDroid, точные URL), PERF-CHECKLIST (4 замера с командами и порогами: PSS 30 МБ, старт 400 мс, аллокации 0, janky ~0%).</done>
</task>

<task type="auto">
  <name>Task 4: Структурная верификация фазы + bookkeeping (PERF-01 механически, REL-01 подтверждён, REL-02/03 подготовлены)</name>
  <read_first>
    - .planning/REQUIREMENTS.md — PERF-01..03 (:87-89), REL-01..03 (:94-96), Traceability (:169-175)
    - .planning/STATE.md — формат decision [NN-01], Blockers (standing-паттерн деферрала)
    - .planning/phases/10-onbording-i-nastroyki/10-01-PLAN.md — Task 4 (прецедент структуры верификации)
  </read_first>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
    1. **Полный прогон сборок:** `./gradlew assembleDebug && ./gradlew assembleRelease` зелёные; `bash scripts/check-no-internet.sh` (source) + по release-APK — exit 0.
    2. **Fail-capable грепы фазы (пиновка — падают при регрессии):**
       - PERF-01: release-APK `stat` ≤ 3 145 728 байт (число зафиксировать в SUMMARY);
       - shrink-линия: `shrinkResources true` в build.gradle И `res/raw/keep.xml` существует И aapt2 dump release: `locale_name` ≥ 8, `keyboard_layout_set_tatar` есть;
       - ребрендинг-линия: `>Tatar Keyboard<` в strings-appname.xml, «(dev)» отсутствует; `name="setup_message"` отсутствует в values/strings.xml;
       - REL-01: signingConfigs-линия жива (`keystore.properties` в build.gradle + `signingConfig signingConfigs.release`) И `apksigner verify` release-APK OK;
       - PERF-04-инвариант: `android.permission.INTERNET` отсутствует в манифесте;
       - CI-линия: `assembleRelease` И `3145728` в ci.yml;
       - версия: `"1.0.0"` в build.gradle.
    3. **Boundary-check (механический, от 7f99505):** `git diff --name-only 7f99505..HEAD -- app/ .github/` = ровно 5 файлов (app/build.gradle, app/src/main/res/raw/keep.xml, app/src/main/res/values/strings-appname.xml, app/src/main/res/values/strings.xml, .github/workflows/ci.yml); ни одного .java/.kt в диффе (zero-code фаза); values-*/strings.xml (локализации) не тронуты; манифест не тронут.
    4. **Bookkeeping REQUIREMENTS.md** — чек-боксы: **PERF-01 → [x]** (механическое требование, закрыто числом stat) и **REL-01 → [x]** (keystore + подписанный APK верифицированы apksigner). PERF-02/03 и REL-02/03 чек-боксы НЕ ставить (device/публикация). Аннотации (дата исполнения) под каждым из 6:
       - PERF-01: *«Release-APK <факт> байт ≤ 3 145 728 (запас ~4.6×) при minifyEnabled+shrinkResources+keep.xml (locale_name_* и layout-ресурсы защищены от стрипа — живой эксперимент ресерча). Гейт продублирован в CI (stat в ci.yml). См. 11-RESEARCH.md §1, §6.»*
       - PERF-02: *«Deferred device UAT: adb-команды и пороги (PSS ≤ 30 МБ ×3 замера, холодный старт < 400 мс медиана ×5) — 11-PERF-CHECKLIST.md. Прогнать на бюджетном устройстве (Xiaomi/Redmi или Samsung A).»*
       - PERF-03: *«Deferred device UAT: Profiler (0 аллокаций onDraw/onTouchEvent, 0 GC при печати) + gfxinfo (janky ≤ 1%) — 11-PERF-CHECKLIST.md. Дисциплина заложена фазой 6 (3 PERF-фикса горячего пути).»*
       - REL-01: *«Keystore существует с фазы 1 (release.jks RSA-4096/SHA-384, alias tatarkeyboard, до 2056; gitignored, в истории git отсутствует); release-APK подписан и верифицирован apksigner verify. Бэкап jks вне репо — шаг 1 PUBLISH-CHECKLIST (standing blocker фазы 1). См. 11-RESEARCH.md §4.»*
       - REL-02: *«Подготовлено: PRIVACY.md (данные не собираются, no-INTERNET проверяемо CI, Apache-2.0) + ссылка из README. „Опубликована" = после ручного пуша репо (PUBLISH-CHECKLIST шаги 2–4); URL-плейсхолдеры strings-appname.xml заменяются на реальном owner (шаг 3).»*
       - REL-03: *«Подготовлено: CHANGELOG.md v1.0.0 + docs/PUBLISH-CHECKLIST.md (repo→push→CI+негативный тест→тег v1.0.0→GitHub Release с подписанным APK→заявка IzzyOnDroid, точные URL/команды). Исполнение — строго ручное (locked decision).»*
       Traceability: PERF-01 → `Complete (11-01: mechanical stat gate)`; PERF-02/03 → `Verifying (11-01: checklist prepared; on-device UAT deferred)`; REL-01 → `Complete (11-01: apksigner verified)`; REL-02/03 → `Verifying (11-01: docs prepared; manual publication pending)`.
    5. **Bookkeeping STATE.md** — decision `[11-01]`: shrinkResources включён с keep.xml (R1 снят экспериментом: враг — locale_name_*, не раскладки; маски покрывают 4 getIdentifier call-site), ребрендинг минимальный (english_ime_name → «Tatar Keyboard», setup_message удалён из base, 35 локализаций = backlog), versionName 1.0.0, CI дополнен release+size-гейтом, 5 док-файлов, публикация строго ручная по PUBLISH-CHECKLIST, URL-плейсхолдеры до создания репо. Session Continuity: «Phase 11 complete-local — v1.0 milestone locally done; остались device-UAT бандл (фазы 1–11) + ручная публикация». Blockers: чек-лист Task 5 (см. ниже) присоединить к standing UAT-бандлу.
    6. **Напоминание пользователю (в SUMMARY и финальном выводе executor):** сделать бэкап `release.jks` + `keystore.properties` вне репо ДО публикации — потеря ключа = невозможность обновлений навсегда (шаг 1 PUBLISH-CHECKLIST).
  </action>
  <verify>
    <automated>cd /Users/amirka/workspace/main/development/tatar-keyboard && ./gradlew assembleDebug && ./gradlew assembleRelease && bash scripts/check-no-internet.sh && APK=$(ls app/build/outputs/apk/release/*.apk | head -1) && bash scripts/check-no-internet.sh "$APK" && APKSIZE=$(stat -c%s "$APK" 2>/dev/null || stat -f%z "$APK") && test "$APKSIZE" -le 3145728 && aapt2 dump resources "$APK" | grep -c 'locale_name' | xargs -I{} test {} -ge 8 && aapt2 dump resources "$APK" | grep -q 'keyboard_layout_set_tatar' && grep -q 'shrinkResources true' app/build.gradle && grep -q '"1.0.0"' app/build.gradle && grep -q 'keystore.properties' app/build.gradle && grep -q 'signingConfig signingConfigs.release' app/build.gradle && test -f app/src/main/res/raw/keep.xml && grep -q '>Tatar Keyboard<' app/src/main/res/values/strings-appname.xml && ! grep -q '(dev)' app/src/main/res/values/strings-appname.xml && ! grep -q 'name="setup_message"' app/src/main/res/values/strings.xml && ! grep -q 'android.permission.INTERNET' app/src/main/AndroidManifest.xml && grep -q 'assembleRelease' .github/workflows/ci.yml && grep -q '3145728' .github/workflows/ci.yml && D=$(git diff --name-only 7f99505..HEAD -- app/ .github/) && test "$(echo "$D" | wc -l | tr -d ' ')" = "5" && echo "$D" | grep -q 'app/build.gradle' && echo "$D" | grep -q 'raw/keep.xml' && echo "$D" | grep -q 'strings-appname.xml' && echo "$D" | grep -q 'values/strings.xml' && echo "$D" | grep -q 'ci.yml' && ! echo "$D" | grep -qE '\.(java|kt)$' && ! echo "$D" | grep -q 'AndroidManifest' && test "$(grep -c 'Verifying (11-01' .planning/REQUIREMENTS.md)" = "4" && test "$(grep -c 'Complete (11-01' .planning/REQUIREMENTS.md)" = "2" && grep -q '^- \[x\] \*\*PERF-01\*\*' .planning/REQUIREMENTS.md && grep -q '^- \[x\] \*\*REL-01\*\*' .planning/REQUIREMENTS.md && test "$(grep -cE '^- \[ \] \*\*(PERF-0[23]|REL-0[23])\*\*' .planning/REQUIREMENTS.md)" = "4" && grep -q '\[11-01\]' .planning/STATE.md && grep -qi 'release.jks' .planning/STATE.md</automated>
  </verify>
  <done>Обе сборки + оба уровня check-no-internet зелёные; PERF-01 закрыт числом (stat ≤ 3 145 728, факт в SUMMARY); shrink-линия запинована (keep.xml + aapt2 locale_name ≥ 8 + layout set tatar); REL-01 закрыт (signingConfigs жив + apksigner OK); ребрендинг запинован («Tatar Keyboard» без dev, setup_message удалён); CI-линия запинована; boundary 7f99505 = ровно 5 файлов без .java/.kt/манифеста; REQUIREMENTS: PERF-01+REL-01 [x] + 6 аннотаций + Traceability 2×Complete 4×Verifying; STATE decision [11-01] + напоминание о бэкапе jks.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Device-UAT PERF-02/03 + финальный QA + ручная публикация (REL-03)</name>
  <read_first>
    - .planning/phases/11-proizvoditelnost-i-reliz/11-PERF-CHECKLIST.md — команды и пороги замеров (создан Task 3)
    - docs/PUBLISH-CHECKLIST.md — шаги публикации (создан Task 3)
    - .planning/STATE.md — Blockers: UAT-бандл фаз 1–10 (прогнать заодно)
  </read_first>
  <what-built>v1.0 локально готов: release-APK 681 078 байт (подписан, shrink+keep, «Tatar Keyboard» 1.0.0), CI с size-гейтом, README/PRIVACY/CHANGELOG/PUBLISH-CHECKLIST/PERF-CHECKLIST. Осталось три ручных блока: замеры на устройстве, финальный QA, публикация.</what-built>
  <action>
    Стандартная отложенная схема (standing-паттерн фаз 1–10): если устройство недоступно — блоки (a)/(b) откладываются в STATE.md Blockers self-contained чек-листом, фаза закрывается «complete-local», executor закрывает фазу в один проход. Блок (c) публикации ВСЕГДА остаётся ручным — независимо от наличия устройства.

    **(a) PERF-02/03 замеры** — по 11-PERF-CHECKLIST.md на бюджетном устройстве (Xiaomi/Redmi или Samsung A-серия), release-APK:
    1. PSS: `adb shell dumpsys meminfo org.tatarkeyboard.ime` при показанной клавиатуре — TOTAL PSS ≤ 30 МБ (×3: после открытия, после ротации, после цикла tt→ru→en).
    2. Холодный старт: force-stop → тап в поле → показ < 400 мс (медиана ×5, методика в чек-листе).
    3. Profiler: 30 с печати — 0 аллокаций из onDraw/onTouchEvent, 0 GC-событий.
    4. gfxinfo: reset → 60 с печати → Janky frames ≤ 1%.
    Числа вписать в 11-PERF-CHECKLIST.md (таблица факт/PASS) → чек-боксы PERF-02/03 в REQUIREMENTS.md.
    **(b) Финальный QA v1.0:** чистая установка release-APK (`adb uninstall org.tatarkeyboard.ime` → install подписанного) → онбординг от иконки до «ә» в Telegram (SC3 фазы 10) → smoke всех фич CHANGELOG: пятый ряд, глобус tt/ru/en, long-press дубли, shift/caps, backspace-удержание, double-space, свайп-курсор, слои ?123/#+=, баллон/панель, звук/вибро тумблеры, password-поле, ландшафт, тёмная тема. Заодно — UAT-бандл фаз 1–10 из STATE.md Blockers (полные чек-листы там).
    **(c) Публикация (REL-03, строго вручную):** по docs/PUBLISH-CHECKLIST.md шаг за шагом: бэкап jks (шаг 1 — сделать ДАЖЕ если публикация откладывается) → GitHub-репо → URL-фикс strings-appname.xml → push → зелёный CI + красный негативный тест PERF-04 → тег v1.0.0 → GitHub Release с подписанным APK → заявка IzzyOnDroid. После публикации: чек-боксы REL-02/03 в REQUIREMENTS.md, Traceability → Complete, v1.0 milestone закрыт.
  </action>
  <verify>
    Подтверждение пользователя по блокам (a) замеры PERF-02/03 в бюджете, (b) QA пройден, (c) публикация выполнена (или бэкап jks сделан + публикация сознательно отложена); либо честная запись деферрала в STATE.md Blockers по образцу фаз 1–10
  </verify>
  <done>PERF-02/03 замерены и в бюджете + QA пройден + релиз опубликован (REL-03) — либо блоки честно отложены в STATE.md Blockers (фаза complete-local, v1.0 «locally done», бэкап jks напомнен отдельно)</done>
  <resume-signal>Type "approved" по блокам (a)/(b)/(c), либо описать проблемы/замеры вне бюджета; при недоступном устройстве/отложенной публикации — подтвердить деферрал (запись в STATE.md Blockers)</resume-signal>
</task>

</tasks>

<verification>

1. `./gradlew assembleDebug` и `./gradlew assembleRelease` exit 0; `check-no-internet.sh` (source + release-APK) exit 0.
2. **PERF-01 механически:** release-APK `stat` ≤ 3 145 728 байт — число зафиксировано.
3. **Shrink-линия (R1):** `shrinkResources true` + `res/raw/keep.xml` с масками locale_name_*/label_*/layout-ресурсов; aapt2 dump release: locale_name ≥ 8, keyboard_layout_set_tatar есть, label_pause/wait = 2 — фатальный стрип имён локалей исключён.
4. **REL-01:** signingConfigs-цепочка в build.gradle жива, `apksigner verify` release-APK OK.
5. **Ребрендинг:** english_ime_name = «Tatar Keyboard» (без «(dev)»); setup_message отсутствует в values/strings.xml; локализации values-*/ и манифест не тронуты.
6. **CI:** ci.yml содержит assembleRelease (unsigned) + size-гейт `stat ... -le 3145728`; YAML валиден.
7. **Документы:** README (без placeholder-скриншотов/мёртвых бейджей/iOS-маркетинга) + PRIVACY + CHANGELOG (v1.0.0) + docs/PUBLISH-CHECKLIST (точные команды, бэкап jks шагом 1) + 11-PERF-CHECKLIST (4 замера, команды+пороги) — все существуют, контент-грепы PASS.
8. **Boundary, fail-capable:** дифф `7f99505..HEAD -- app/ .github/` = ровно 5 файлов (build.gradle, raw/keep.xml, strings-appname.xml, values/strings.xml, ci.yml); ни одного .java/.kt; манифест нетронут.
9. **Bookkeeping:** PERF-01 + REL-01 → [x]; PERF-02/03 + REL-02/03 → [ ] с аннотациями; Traceability ×6 обновлена (2 Complete, 4 Verifying); STATE decision [11-01]; напоминание о бэкапе jks выдано.
10. Human-verify чекпойнт (Task 5: замеры + QA + публикация) пройден или честно отложен в STATE.md Blockers.

</verification>

<success_criteria>

- SC1 (PERF-01, REL-01): подписанный release-APK ≤ 3 МБ — замер числом PASS, apksigner verify PASS.
- SC2 (PERF-02): PSS ≤ 30 МБ + холодный старт < 400 мс — чек-лист с командами готов, замеры deferred device UAT (Task 5a).
- SC3 (PERF-03): 0 аллокаций/GC, janky ~0% — чек-лист готов, замеры deferred device UAT (Task 5a).
- SC4 (REL-02): PRIVACY.md написана и слинкована из README — PASS локально; «опубликована» — после ручного пуша (Task 5c).
- SC5 (REL-03): GitHub Release + заявка IzzyOnDroid — инструкция с точными командами готова (PUBLISH-CHECKLIST), исполнение строго ручное (Task 5c).
- Инварианты: no-INTERNET (PASS оба уровня), ноль новых зависимостей (boundary PASS), ноль .java/.kt-правок (zero-code фаза), keystore не в git (установлено фазой 1, не трогается).

</success_criteria>

## Flagged assumptions

- **A1 (aapt2 в PATH, LOW):** aapt2 доступен локально — подтверждено ресерчем (дампы делались) и фазами 2–3 (aapt2-гейты). Если исполнителю недоступен как голая команда — использовать `$ANDROID_HOME/build-tools/<ver>/aapt2` (тот же паттерн, что apksigner-фолбэк в Task 1).
- **A2 (имя unsigned-APK в CI, LOW):** AGP при отсутствующем signingConfig кладёт `app-release-unsigned.apk`. Если имя отличается в конкретной версии AGP — CI-шаг упадёт на первом прогоне (репо ещё не создано, прогонов не было); фикс = поправить имя файла в ci.yml при публикации, отмечено в PUBLISH-CHECKLIST шаге 5 (зелёный CI обязателен до тега).
- **A3 (стабильность размера, LOW):** 681 078 байт — замер ресерча с shrink+keep; ребрендинг-правки (минус одна строка, минус « (dev)») размер только уменьшают. Гейт ≤ 3 145 728 проходит с запасом 4.6×.
- **A4 (orphan-локализации setup_message, LOW):** 35 values-*/strings.xml с setup_message без базовой строки — aapt2 такие переводы игнорирует; сборка зелёная (проверено логикой AGP: translation without default → warning, не error, для non-translatable отсутствующих). Если вопреки ожиданию сборка упадёт — откатить удаление setup_message (оставить строку с нейтральным текстом «Tatar Keyboard») и записать в STATE; чистка 35 файлов остаётся backlog.
- **A5 (база диффа):** 7f99505 — текущий HEAD (docs-коммит ресерча). Коммит этого плана — только .planning/ — boundary-чек по app/ + .github/ корректен (прецедент фаз 8–10).
- **A6 (дата в CHANGELOG):** исполнение фазы ожидается 2026-07-19 (по датам CONTEXT/RESEARCH); executor ставит фактическую дату исполнения.

## Prohibition recall (две стадии — запись для фазы)

Stage 1 (adversarial, over-produce): «раз уж финальная фаза» — создать GitHub-репо и запушить «чтобы CI сразу прогнался»; выполнить `gh release create` автоматически «раз всё готово»; закоммитить keystore.properties «для удобства CI-подписи»; поднять minSdk до 26 «раз перед релизом надо уточнить»; переребрендить все 35 локализованных setup_message «раз взялись за бренд»; заменить ic_launcher «новому имени — новая иконка»; добавить бейджи CI/IzzyOnDroid в README «красивее»; вставить скриншоты-заглушки «потом заменим»; конвертировать versionCode в auto-increment «по-взрослому»; добавить aapt2-гейт в CI с установкой build-tools «полная копия локального гейта»; подключить Firebase Crashlytics «узнать о крашах после релиза».

Stage 2 (precision) — kept: (1) **NO push/publish автоматически, NO создания GitHub-репо** → authored здесь (prohibition — locked decision CONTEXT «публикацию выполнить вручную»; PUBLISH-CHECKLIST = инструкция, не скрипт; Task 5c строго ручной); (2) **keystore не коммитится** → authored здесь (T-11-01 critical; gitignore-статус установлен фазой 1, фаза его не трогает; CI работает unsigned by design); (3) **minSdk 24 финален, ноль gradle-бампов** → authored здесь (locked decision этого прогона; дифф build.gradle = ровно 2 строки: shrinkResources + versionName); (4) **no-INTERNET/no-аналитики** → authored здесь (PERF-04-инвариант, T-11-03: PRIVACY.md обещает — манифест доказывает); (5) **no-скриншоты-заглушки/no-мёртвые-бейджи в README** → authored здесь (уникальная граница фазы: IzzyOnDroid-ревью читает README; мёртвый бейдж/заглушка = красный флаг; негатив-грепы Task 3). Canon-referral drops: 35 локализаций setup_message + ic_launcher — STATE backlog (post-v1.0); Kotlin-конвертация — CLAUDE.md; PERF-02/03 на устройстве — Task 5/UAT-бандл (не выдумывать числа без устройства).

## Artifacts this phase produces (plan 11-01)

- **Build-конфиг:** `app/build.gradle` (+shrinkResources, versionName 1.0.0), `app/src/main/res/raw/keep.xml` (новый — маски 4 getIdentifier call-site)
- **Ресурсы:** `strings-appname.xml` (минус «(dev)»), `values/strings.xml` (минус setup_message)
- **CI:** `.github/workflows/ci.yml` (+assembleRelease unsigned, +size-гейт 3145728)
- **Документы (корень):** `README.md` (переписан), `PRIVACY.md` (расширен), `CHANGELOG.md` (новый), `docs/PUBLISH-CHECKLIST.md` (новый)
- **Phase-док:** `11-PERF-CHECKLIST.md` (adb-замеры PERF-02/03 для device-сессии)
- **Planning:** REQUIREMENTS.md (PERF-01+REL-01 [x], 6 аннотаций, Traceability ×6), STATE.md (decision [11-01], Blockers-чеклист Task 5), 11-01-SUMMARY.md при исполнении
- **Не создаётся/не меняется:** GitHub-репо/remote/теги (строго ручное — Task 5c), keystore-файлы, AndroidManifest.xml, любые .java/.kt, values-*/strings.xml (35 локализаций — backlog), ic_launcher (backlog), minSdk/compileSdk/зависимости

<output>
Create `.planning/phases/11-proizvoditelnost-i-reliz/11-01-SUMMARY.md` when done
</output>

