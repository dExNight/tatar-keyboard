---
milestone: v1.0
audited: 2026-07-19
verdict: PASSED (complete-local)
auditor: orchestrator (integration-checker failed twice on transient API error — audit run directly)
requirements_total: 34
requirements_verified_checked: 30
requirements_deferred_open: 4
---

# Milestone v1.0 — Audit

**Verdict: ✅ PASSED (complete-local).** All 11 phases delivered and verified within what is mechanically provable without a physical device and without a published GitHub repository. The 4 open requirements are honestly deferred (device measurement + manual publication), not dropped. No constraint violations. No requirement silently unaccounted.

## 1. Requirement coverage (34 total)

**30 checked [x]** — mechanically or structurally verified across phases 1–11.
**4 open [ ]** — genuinely unverifiable in this environment; corrected during audit (phase.complete had auto-checked them; reverted to match their own "deferred/pending" traceability rows):

| ID | Status | Why open |
|----|--------|----------|
| PERF-02 | deferred | PSS ≤30 MB + cold-start <400 ms — needs budget device; adb commands ready in 11-PERF-CHECKLIST.md |
| PERF-03 | deferred | 0 alloc/GC/jank during typing — needs Profiler+gfxinfo on device (discipline written into code phases 6–7, measurement pending) |
| REL-02 | prepared | PRIVACY.md written + linked; "опубликована" true only after manual repo push |
| REL-03 | prepared | GitHub Release + IzzyOnDroid — strictly manual per docs/PUBLISH-CHECKLIST.md |

Categories a/b/c: (a) mechanically verified+checked — layout/input/UI/compat/a11y/setup requirements + PERF-01, PERF-04, REL-01; (b) device-deferred — PERF-02/03; (c) prepared-manual — REL-02/03.

## 2. Cross-phase integration (spot-checked in the signed release APK)

All verified present in `app-release.apk` via `aapt2 dump resources` + `dexdump`:
- `keyboard_layout_set_tatar` + `keyboard_layout_set_russian` (phases 2–3) ✓
- `rowkeys_tatar_extra` — fifth row ә ө ү җ ң һ (phase 2) ✓
- `ios_key_normal` + `ios_key_preview_background` — theme id=7 + balloon (phases 6–7) ✓
- `locale_name_tt_RU` — survived shrinkResources via keep.xml (the R1 crash risk) ✓
- `setup_activity` layout (phase 10) ✓
- KeyDescriptionMapper / KeyboardAccessibilityDelegate / SetupActivity — 15 class refs in dex (phases 9–10) ✓
- versionName 1.0.0 / versionCode 1 / applicationId org.tatarkeyboard.ime ✓

Integration chain intact: layouts (2) → moreKeys (3) → shift/mechanics (4–5) → all render through theme id=7 (6) with in-layer preview/panel (7) → a11y delegate maps the same keys (9) → SetupActivity routes to SettingsActivity (10) → all packaged in signed release (11).

Note: `keep.xml` count = 0 in APK is EXPECTED (shrinker directive consumed at build, not packaged); the `spoken_description_*` probe used a wrong guessed name (real names verified in phase 9).

## 3. Constraint compliance sweep

| Constraint | Result |
|------------|--------|
| No INTERNET | ✓ 0 in manifest; check-no-internet green on both debug + release APK (CI enforces per-commit) |
| APK ≤ 3 MB | ✓ 681,070 bytes (4.6× headroom) |
| Apache-2.0 | ✓ LICENSE intact (Apache 2.0), AOSP headers preserved, upstream Simple Keyboard credited in metadata; no NOTICE upstream → §4(d) N/A |
| No Apple assets | ✓ icons are own VectorDrawable; no SF Pro/SF Symbols; no iPhone/iOS marketing in README/CHANGELOG/PRIVACY |
| Layouts/themes as data | ✓ milestone Java/Kotlin diff limited to accepted files (double-space restore ph5, PERF fixes + a11y ph6/9, SetupActivity ph10) |
| Keystore not in git | ✓ `git log --all --full-history -- '*.jks' 'keystore.properties'` EMPTY; both gitignored |
| minSdk | ✓ 24 (locked decision) |

## 4. Consolidated deferred-work ledger

**(a) Device UAT bundle** (phases 1–11, in STATE.md Blockers) — one combined pass when a budget device (Xiaomi/Redmi or Samsung A) is available:
- Layouts/input: fifth row tap+shift, moreKeys dubli (ё/ъ), autocap, codepoint backspace+repeat, Enter actions, double-space→period+revert, space-swipe cursor, two-finger no-loss
- UI: palette light/dark + caps-lock highlight, theme live-reload, balloon on ACTION_DOWN incl. edges, slide-select, haptic/sound
- Compat: password, WebView keyCode 229, edge-to-edge API 35+, landscape (no extract mode), Direct Boot, MIUI/One UI
- A11y: TalkBack explore-by-touch «татарская э», lift-to-type «әни» sent in messenger, non-TalkBack not degraded
- Setup: clean install icon→«ә», sound/vibration toggles live
- PERF-02/03: PSS, cold-start, Profiler GC-free, gfxinfo janky — per 11-PERF-CHECKLIST.md

**(b) Publication** (REL-02/03) — manual per docs/PUBLISH-CHECKLIST.md: back up release.jks FIRST → create GitHub repo → push → green CI + red negative test → tag v1.0.0 → GitHub Release with APK → IzzyOnDroid request → swap `.invalid` placeholder URLs.

**(c) Post-v1.0 backlog** (STATE.md): progressive backspace acceleration; moreKeys panel a11y (ё/ъ unreachable via TalkBack); values-tt a11y strings; full "Simple Keyboard" rebrand across 44 locale strings.xml + orphaned setup_message translations; shrinkResources orphan-translation cleanup; swapped titles in app_restrictions.xml (upstream bug).

## 5. Documentation

README.md, PRIVACY.md, CHANGELOG.md, docs/PUBLISH-CHECKLIST.md, 11-PERF-CHECKLIST.md all present. Version 1.0.0 consistent across CHANGELOG + PUBLISH-CHECKLIST + build.gradge (README uses relative release links, no hardcoded version — acceptable). metadata/en-US rebranded to Tatar Keyboard; stale pt-BR removed.

## Audit integrity note

One correction applied during audit: PERF-02/03 + REL-02/03 were auto-checked `[x]` by phase.complete but are not actually satisfied (device + publication pending). Reverted to `[ ]` to keep the checklist honest and consistent with their traceability rows. This is the milestone's single bookkeeping fix; no code or verification gaps found.

## Addendum: Post-audit device-UAT (2026-07-20)

Первый прогон на реальном устройстве (Samsung, One UI) выявил краш SettingsActivity и пикера языков **во всех билдах v1.0.0** (LocaleResourceUtils: getIdentifier по namespace вместо applicationId → getString(0) crash). Вердикт **PASSED (complete-local)** относился к структурной верификации без устройства — этот класс runtime-багов был вне её досягаемости; сам вердикт в своих рамках остаётся корректным. Баги исправлены (вместе с персистентностью subtype после смерти процесса и устойчивостью SettingsActivity к внешним интентам); **релизным артефактом становится 1.0.1 (versionCode 2)** — v1.0.0 не публикуется. Детали — docs/FIXES-1.0.1.md.
