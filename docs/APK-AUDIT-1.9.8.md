# Аудит артефакта 1.9.8

Миссии «разговорный корпус», части A и B: обе таблицы биграмм перепакованы со
смешанным обучением (новостные Leipzig + дедуплицированные разговорные
Tatoeba/OpenSubtitles). Ветка `main`, база — релиз-кандидат 1.9.7
(`dist/tatar-keyboard-1.9.7.apk`, versionCode 23). Файл:
`dist/tatar-keyboard-1.9.8.apk`. Отчёты миссий с замерами —
`docs/CORPUS-CONVERSATIONAL-RU.md` и `docs/CORPUS-CONVERSATIONAL-TT.md`.

## Одной таблицей

| | |
|---|---|
| versionName / versionCode | **1.9.8 / 24** (было 1.9.7 / 23) |
| Размер APK | **2 095 592 Б** при потолке 3 145 728 Б, запас **33,4 %** (было 2 098 856 Б, **−3 264 Б, −0,2 %**) |
| SHA-256 APK | `a863fe39cb274077376e43cbaa1516fc8a2965930dd898e8dacc6587441a1723` |
| Подпись | тот же ключ: сертификат SHA-256 `98ca6febfed6c146d81c1fdcfe52c79acf7aa926a1033d98b844a59803ec42ad`, как у 1.9.4–1.9.7; CN=Tatar Keyboard |
| Разрешения | одно: `android.permission.VIBRATE`. INTERNET нет |
| compileSdk / targetSdk / minSdk | 37 / 37 / 24 |
| Файлов в APK | **244** (как в 1.9.7) |
| JVM-тесты | **1007**, падений 0 (`--rerun-tasks`) |
| Python-наборы `tests/` | **294** в 11 файлах, падений 0 |
| `lintRelease` | зелёный с baseline |
| no-INTERNET gate | exit 0 на исходнике (debug) и на релизном APK |
| Воспроизводимость | два `clean assembleRelease` одного дерева → **одинаковый SHA-256** (`a863fe39…1723`) |
| Ради чего версия | обе таблицы биграмм дообучены на разговорном корпусе: 57 русских слов и все шесть названных татарских повелений заговорили |

## Дельта по компонентам (release_check, 1.9.7 → 1.9.8)

| Компонент | 1.9.7, Б | 1.9.8, Б | Δ |
|---|---:|---:|---:|
| `assets/` | 1 797 136 | 1 795 998 | **−1 138** |
| `classes.dex` | 418 812 | 418 812 | 0 |
| `res/` | 431 918 | 431 918 | 0 |
| `resources.arsc` | 92 664 | 92 984 | +320 (versionCode/versionName) |
| `AndroidManifest.xml` и прочее | 4 864 | 4 864 | 0 |
| **Всего (несжатое)** | 2 745 394 | 2 744 576 | −818 |
| **APK (сжатый)** | 2 098 856 | **2 095 592** | **−3 264** |

Внутри `assets/` изменились ровно три файла (пофайловая сверка распакованных
APK): русская таблица 152 947 → **149 118** (−3 829), татарская таблица
175 843 → **176 749** (+906), `assets/bigrams/NOTICE.txt` 3 158 → 4 943
(+1 785 — атрибуция разговорных источников обеих таблиц). Кода в релизе нет
(дельта dex = 0): правки миссий — ассеты, пины, тесты, документы, бамп версии.

## Сверка пинов ассетов

Все 16 значений (размер + SHA-256, сжатый и raw, ×4 ассета) совпали с константами
`DictionaryStorageContracts.kt` / `BigramStorageContracts.kt` — гейт
`artifact.asset_pins` в `scripts/release_check.sh`. Перезакреплены обе таблицы:

| Ассет | Сжатый размер / SHA-256 | Raw размер / SHA-256 |
|---|---|---|
| `bigrams/russian_bigrams_v1.tatbigr.zlib` | 149 118 / `e2f41c05…be4a` | 465 610 / `5e6c3e01…f15e` |
| `bigrams/tatar_bigrams_v1.tatbigr.zlib` | 176 749 / `3a25ea63…8857` | 520 892 / `6db06331…c966` |

**Словари не тронуты**: SHA-256 файлов на диске посчитаны и совпали с пинами —
`tatar_top100k_v1.tdict.zlib` 601 118 / `76bd5a39…e7ac`, `russian_top100k_v1.tdict.zlib`
638 758 / `236f21ea…de47`; в распакованных APK 1.9.7 и 1.9.8 оба файла байт-в-байт
одинаковые.

## Подпись и разрешения

```
$ apksigner verify --print-certs dist/tatar-keyboard-1.9.8.apk
V2 Signer: certificate DN: CN=Tatar Keyboard
V2 Signer: certificate SHA-256 digest: 98ca6febfed6c146d81c1fdcfe52c79acf7aa926a1033d98b844a59803ec42ad
```

Тот же сертификат `98ca6feb…42ad`, что у 1.9.4–1.9.7 и всей линейки с 2026-08-18.

```
$ aapt2 dump permissions dist/tatar-keyboard-1.9.8.apk
uses-permission: name='android.permission.VIBRATE'
```

## Смоук

`scripts/emulator-smoke.sh --no-boot --avd tt_suggest_a14 --apk dist/tatar-keyboard-1.9.8.apk`:
**15 PASS / 0 FAIL / 4 SKIP** — тот же профиль, что у 1.9.6/1.9.7 (skip'ы by
design: подсказки opt-in нечитаемы на не-debuggable, у en нет словаря).
Свидетельства — `build/emulator-smoke-1.9.8/`.

Полоса предсказаний проверена отдельно на том же AVD (prefs через `adb root`,
userdebug-образ), снимки — `docs/corpus-conversational/evidence/release-198-*.png`:

| Ввод | Полоса | Сверка с ассетом |
|---|---|---|
| tt `ул шалтырат ` | **һәм** | `шалтырат` → һәм (единственный преемник, одна пара из разговорного входа) ✓ |
| tt `кил ` | **дә · әле · һәм** | `кил` → дә·әле·һәм ✓ (в 1.9.7 было әле·дә·син — тройка изменилась перепаковкой части B, зафиксировано в её отчёте) |
| ru `ну давай ` | **я · не · просто** | `давай` → я·не·просто ✓ (голова — последнее слово) |
| ru `я позвоню ` | **в · тебе · ему** | `позвоню` → в·тебе·ему ✓ |

Все четыре тройки совпадают с содержимым ассетов, прочитанным декодером
(`scripts/bigram_asset_pack.py`).

**Обновление на устройстве (живой прогон 1.9.7 → 1.9.8, тот же AVD):**
`install -r` прошёл, `firstInstallTime` не изменился (2026-08-24 18:35:36,
versionCode 23 → 24). Имена файлов в device-protected storage несут raw SHA-256,
поэтому:

- `bigrams/`: появился `…-6db06331…c966.tatbigr` (новая tt-таблица), рядом
  остался `…-d2345b48…22a6.tatbigr` (таблица 1.9.7 — retention
  `MAX_FINAL_ARTIFACTS` = 2, более старый файл вытеснен);
- `bigrams-ru/`: появился `…-5e6c3e01…f15e.tatbigr` (новая ru-таблица), рядом
  `…-0091be8b…690d.tatbigr` (таблица 1.9.7);
- `dictionaries/` и `dictionaries-ru/`: имена прежние
  (`…-8f434ec7…5f76.tdict`, `…-60b30371…faee.tdict`) — словари НЕ
  переинфлировались, как и должно быть при нетронутых пинах.

Обе новые таблицы распаковались ровно один раз при первой активации своего
сабтипа — штатный путь, не регрессия.

## Сборка и воспроизводимость

Два подряд `clean assembleRelease` одного дерева дали побайтно одинаковый APK:
SHA-256 `a863fe39…1723`, размер 2 095 592 Б (`cmp` без различий). В релиз
вкладывать файл из `dist/`.

## Чего в этом аудите нет

* **PSS и холодный старт не мерены** — код не менялся (dex = 0), суммарный
  вес таблиц вырос на 1 785 Б NOTICE и −2 923 Б таблиц; ухудшения ожидать
  неоткуда, но замер остаётся долгом живого устройства.
* **Живого телефона нет** — все числа эмуляторные.
* **В стор ничего не ушло.** Ни `git push`, ни тега наружу, ни релиза:
  артефакт лежит только в `dist/`.
