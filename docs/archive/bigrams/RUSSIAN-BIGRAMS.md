# Русская таблица биграмм и многоязычный выбор таблицы

Дата сборки и проверки: 2026-08-21. Ветка `codex/version-1.6.0` — та же, где HEAD 1.7.0;
отдельная ветка не отводилась, правки составляют одну тему, а релиз собирает отдельная
миссия. Версия приложения не поднималась, `CHANGELOG.md` не менялся, в `dist/` ничего не
клалось, наружу ничего не отправлялось.

Прямой источник задачи — `docs/RUSSIAN-DICTIONARY.md`, раздел 9 пункт 4: русский словарь
поехал в 1.7.0, а предсказание следующего слова осталось татарским.

---

## 1. Что было не так

`DeviceProtectedBigramPreparation.create` отдавал `null` для любого subtype, кроме
`tt_RU`. На русской раскладке `NEXT_WORD` поэтому **всегда возвращал пустой список** —
ровно та же fail-closed картина, что при отсутствующей или испорченной таблице. Ошибки
не было, была тишина.

Это была не только техническая недоделка. Строка настройки после 1.7.0 стала
языконейтральной и обещает «Show **suggested and predicted** words above the keyboard as
you type, **in Tatar and in Russian**» (`tatar_suggestions_summary`), то есть обещала
предсказание для обоих языков, а имела его у одного. Миссия закрывает расхождение первым
способом: предсказание появляется для русского, и строка становится правдой без правки.

---

## 2. Один резолвер языка на оба артефакта

Главное архитектурное требование этой работы, и оно решено ДО того, как появились данные.

До неё «какой сейчас язык» спрашивали **два независимых места**: `DictionaryArtifactSpec.
forSubtype(subtypeId)` — для словаря, и `if (subtypeId != PersonalSubtypes.TATAR_RU)` — для
таблицы биграмм. Пока язык был один, разницы не было. С двумя языками это две копии одного
правила, и починка, положенная в одну копию, молча не ложится в другую.

Теперь копия одна. Таблица биграмм стала **полем языка**, а не отдельно резолвимым
артефактом:

```kotlin
data class DictionaryArtifactSpec(
    …
    val bigrams: BigramArtifactSpec? = null,
) {
    init { require(bigrams == null || bigrams.subtypeId == languageTag) }

    companion object {
        val ALL = listOf(TATAR_TOP100K_V1, RUSSIAN_TOP100K_V1)
        fun forSubtype(subtypeId: String) = ALL.firstOrNull { it.languageTag == subtypeId }
        fun bigramsForSubtype(subtypeId: String) = forSubtype(subtypeId)?.bigrams
    }
}
```

Из этого следует три вещи, каждая проверена тестом:

* таблица не может принадлежать языку, отличному от своего словаря, — `init` этого не
  допустит;
* таблица не может существовать у языка без словаря — она поле его спеки;
* третий язык добавляется одной записью в `ALL`, а не ещё одним `if` в фабрике.

`SuggestionsController` спрашивает `DictionaryArtifactSpec.bigramsForSubtype(subtypeId)`
там же, где спрашивал словарь, и тест
`theTableSeamIsAlwaysAskedForTheSameSubtypeAsTheDictionarySeam` сравнивает два списка
запрошенных subtype поэлементно.

### `fileLanguageTag` — не то же самое, что `subtypeId`

Единственная неочевидная деталь. Татарская таблица уехала в 1.6.0 под именем
`tatar_bigrams-**tt**-v000001-s2-f1-<sha>.tatbigr`, а subtype татарской раскладки —
`tt_RU`. Это разные строки, и склеить их нельзя: подстановка `tt_RU` в имя файла заставила
бы каждое устройство распаковать таблицу заново, да ещё и не прошла бы через
`finalFilePattern`, где на месте языка стоит `[a-z]{2,3}`.

Поэтому у `BigramArtifactSpec` два поля с разными обязанностями:

| Поле | Татарская | Русская | Что делает |
|---|---|---|---|
| `fileLanguageTag` | `tt` | `ru` | попадает в имя файла; для `tt` **заморожено** |
| `subtypeId` | `tt_RU` | `ru` | участвует в выборе таблицы, и только он |

Для русского обе строки совпадают (`PersonalSubtypes.RUSSIAN == "ru"`), и это совпадение,
а не правило: у третьего языка они снова могут разойтись.

### Семейство и каталог — как у словарей

`BigramArtifactSpec` получил `family` и `storageDirectoryName` ровно с той же семантикой и
по тем же двум причинам, что `DictionaryArtifactSpec` в прошлой миссии:

* **обратная совместимость** — татарское `family = "tatar_bigrams"` и каталог `bigrams`
  заморожены, файл на устройстве не переименовывается и не распаковывается заново;
* **учёт аренд** — `ProcessBigramStorageOwner` ключует общее состояние каноническим путём
  каталога, поэтому два семейства в одном каталоге делили бы один счётчик аренд, и второй
  язык невозможно было бы активировать, пока первый держит аренду. Русская таблица взяла
  своё: `family = "russian_bigrams"`, каталог `bigrams-ru`.

`AtomicBigramStore` перестал держать имя семейства в константах: префикс временных файлов и
шаблон итоговых имён он берёт из спеки и требует, чтобы все поддерживаемые артефакты одного
стора принадлежали одному семейству и одному каталогу — тот же `require`, что у
`AtomicDictionaryStore`. Константы `TEMP_PREFIX` и `FINAL_FILE_PATTERN` из него исчезли;
`docs/DICTIONARY-E5B.md` называет вторую по старому имени — это запись от 2026-08-17, и она
не правится.

`AndroidBigramStorageFactory.create` теперь принимает артефакт, а не выбирает его сам.

### Fail-closed сохранён и стал видимым в одном месте

Язык без таблицы по-прежнему **молчит**: `bigramsForSubtype` отдаёт `null`, seam не
создаётся, `NEXT_WORD` отвечает пустым списком, подсказки по префиксу не затронуты. Чужие
предсказания не подставляются никогда.

Досье просило сделать отсутствие «видимым в отладке». Логом это сделать нельзя:
`DictionaryStoragePrivacyTest` и `SuggestionsPackagePrivacyTest` **запрещают
`android.util.Log` в обоих пакетах целиком** — гарантия приватности, которую менять ради
диагностики неправильно. Поэтому отсутствие сделано видимым декларативно: раньше причиной
тишины был `if` внутри фабрики, теперь — `bigrams == null` у записи языка в `ALL`, то есть
в том же списке, где видно и наличие таблицы у соседа. Два случая различимы по этому
списку: язык есть с `bigrams == null` — таблицы нет; subtype в `ALL` отсутствует — нет и
словаря. Тест `everyShippedLanguageResolvesItsOwnTableThroughTheOneRegistry` фиксирует оба.

---

## 3. Конвейер сборки стал многоязычным

`scripts/bigram_pack.py` (прототип-измеритель E5a) и `scripts/bigram_asset_pack.py`
(настоящий упаковщик E5b) получили `--language`, который выбирает алфавит для токенизации и
для чтения поставляемого словаря. Реестр языков не заводился второй раз — берётся тот же
`dictionary_coverage.Language`, что и у словарей (`tat`/`rus`).

**Все точки входа по умолчанию татарские.** `normalized_tokens`, `count_pairs`, `evaluate`,
`read_shipped_vocabulary`, `run_matrix`, `run_pack` — у каждой алфавит/язык последним
параметром со значением по умолчанию, поэтому вызов, написанный до многоязычности, ведёт
себя ровно как раньше, вплоть до текста причины фильтрации. Проверено: татарские наборы
тестов зелёные без единой правки.

Алфавит здесь важнее, чем кажется. Татарский — строгое надмножество русского, так что
токенизация русского текста татарским алфавитом не отбросила бы токен с татарской буквой, а
пропустила бы его как слово — вместо того чтобы разорвать на нём соседство. Слово всё равно
не прошло бы фильтр «оба конца пары в поставляемом словаре», но соседство было бы посчитано
неверно.

Тест `test_no_licensed_sources_are_tracked` расширен с `-words.txt` на `-sentences.txt`:
входные данные таблиц биграмм — именно предложения, и до сих пор их никто не сторожил.

---

## 4. Данные: лицензия, источники, обработка

### Лицензия и атрибуция

Таблица получена из загружаемых русских корпусов
[Leipzig Corpora Collection](https://wortschatz.uni-leipzig.de/en/download/Russian) — тех
же наборов, из которых собран русский словарь.
[Условия использования](https://wortschatz.uni-leipzig.de/en/usage) лицензируют загружаемые
корпуса под [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/).

Рекомендуемая ссылка: D. Goldhahn, T. Eckart, U. Quasthoff, «Building Large Monolingual
Dictionaries at the Leipzig Corpora Collection: From 100 to 200 Languages», LREC 2012.

Данные таблицы остаются отдельным компонентом под CC BY 4.0; Apache-2.0 приложения эту
лицензию не заменяет. Атрибуция обеих таблиц — в `app/src/main/assets/bigrams/NOTICE.txt`,
файл переписан по образцу `app/src/main/assets/dictionaries/NOTICE.txt`: общая шапка,
раздел на артефакт, общая оговорка о лицензии.

### Источники

Ни архивы, ни `*-sentences.txt` в репозиторий не кладутся. До этой миссии за этим следил
тест только по `-words.txt`; теперь `tests/dictionary_pack/test_dictionary_pack.py::
test_no_licensed_sources_are_tracked` ловит и `-sentences.txt`.

| Корпус | Роль | Archive SHA-256 | SHA-256 распакованного `-sentences.txt` |
|---|---|---|---|
| `rus_news_2022_1M` | обучение | `580f19036784bdc48335875c42e91cb7a82bfe48ee5687b663d43d6ef69e7393` | `dedbacd2e06de46afba5561f5e8d528a2d73898288ce8759ede002f00ed8248a` |
| `rus_news_2019_1M` | обучение | `93fed13d5d32bf491149223eac593fbc9fecb4a88e73905455e418a4bdd20012` | `5d42ab9b1428ea59b5eeba14275a272678a4c28f3702db4f27ddea5c1304b5da` |
| `rus_wikipedia_2021_1M` | обучение | `f87d687024ccf3b586aa8b666f2928a369aae9064bfc3b22149087e79ec7ab5b` | `0427676fc54a18afd3b80e0fb7650d8b59a2fa9be5b9d2c77cb50a3a0a5b5f8e` |
| `rus_news_2024_1M` | held-out | `7df13a6ba25c47fde0e156c1ef670fce7a86b126ee9ff9d7246ba840ef964242` | `ebbd52e9d86610b408cb9d054a1a1010dfbd595fdacd999829409a3f0ba4b7da` |

Хеши **архивов** всех четырёх сверены с таблицей в `docs/RUSSIAN-DICTIONARY.md` и совпали
байт в байт: таблица биграмм считается по тем же самым данным, из которых собран словарь, а
не по «похожим». Хеши `*-sentences.txt` измерены здесь впервые — словарю нужны были
`*-words.txt`, таблице нужны предложения; в каждом файле ровно 1 000 000 строк.

Роли распределены как у словаря: два разных года новостей плюс Википедия в обучении,
не участвующий в обучении год новостей — на held-out. Для татарской таблицы роли были
другими (`tat_mixed` + `tat_web` в обучении, `tat_news` — held-out), и не по выбору:
корпусов `mixed`/`web` для русского в каталоге Лейпцига **не существует ни в одном
размере** — это перебрано и записано в `docs/RUSSIAN-DICTIONARY.md`, раздел 2.

### Правила подсчёта пар — те же, что у татарской таблицы

Ни одно правило E5a не менялось, менялся только алфавит:

1. Строка `*-sentences.txt` — ровно два поля через табуляцию, первое — положительный
   десятичный идентификатор. Любая другая строка обрывает сборку, а не пропускается молча.
2. Токены режутся по пробельным промежуткам. Токен, отвергнутый `normalize_word`, **рвёт
   соседство**, а не становится прозрачным: иначе «х , у» дало бы пару (х, у) через знак
   препинания. Плата за это правило — систематическая потеря последнего слова каждого
   предложения и каждой запятой; она записана, а не заглажена.
3. Оба конца пары обязаны быть в поставляемом `russian_top100k_v1` — словарь читается через
   `decompress_asset` + `validate_raw`, то есть из того же самого файла, который лежит в
   APK. Разъехаться с ассетом молча нечему.
4. Пары «слово само с собой» отбрасываются.
5. Головы — самые частотные слова словаря, порядок при равной частоте по возрастанию
   кодовых точек. Преемники внутри головы — по убыванию числа пар, при равенстве по
   возрастанию кодовых точек.
6. Голова, у которой в обучении не нашлось ни одного преемника, **выбрасывается из файла**,
   а не хранится с пустым диапазоном: пустой диапазон валидатор считает порчей.
7. Сжатие — zlib уровень 9, `wbits=15`, `memLevel=9`, один `Z_FINISH`, без preset-словаря.
8. Оба потолка — 250 000 Б сжатого и 1 048 576 Б распакованного — проверяются самим
   генератором, а не только приёмкой.

**Бюджет формата остался один на все языки.** Второй, более щедрый потолок для русского не
заводился — по той же причине, по какой его не завели для русского словаря: второе число
такого рода это приглашение однажды протащить артефакт побольше, не заметив.

---

## 5. Выбор конфигурации: та же матрица, то же правило

`scripts/bigram_pack.py matrix --language rus` прогнал те же семь конфигураций H×K, что E5a
гонял для татарского, на тех же ролях корпусов. Отчёт целиком —
`docs/RUSSIAN-BIGRAMS.matrix.generated.json`; ниже он весь.

Правило выбора взято из E5b без изменений: **наибольший безусловный top-3 hit-rate среди
конфигураций, проходящих оба потолка.**

| H | K | Распакованных Б | Сжатых Б | Потолки | Пар | V | Безусловный top-3 | Условный top-3 | Событий с предсказанием |
|---:|---:|---:|---:|:---:|---:|---:|---:|---:|---:|
| 8 000 | 4 | 405 894 | 127 039 | ok | 31 996 | 4 815 | 12,98 % | 17,01 % | 76,31 % |
| 8 000 | 6 | 512 524 | 170 280 | ok | 47 984 | 6 933 | 12,98 % | 17,01 % | 76,31 % |
| 8 000 | 8 | 617 748 | 212 205 | ok | 63 965 | 8 935 | 12,98 % | 17,01 % | 76,31 % |
| 8 000 | 10 | 722 598 | 252 929 | **сжатый** | 79 941 | 10 898 | 12,98 % | 17,01 % | 76,31 % |
| **10 000** | **6** | **635 838** | **211 070** | **ok** | **59 982** | **8 260** | **13,64 %** | **17,31 %** | **78,77 %** |
| 10 000 | 4 | 505 768 | 159 007 | ok | 39 996 | 5 779 | 13,64 % | 17,31 % | 78,77 % |
| 10 000 | 8 | 763 462 | 261 891 | **сжатый** | 79 958 | 10 566 | 13,64 % | 17,31 % | 78,77 % |

Конфигурация (10 000, 10) в матрице отсутствует — она исключена ещё в E5a
(`EXCLUDED_CORNER`), и это правило тоже не менялось.

Знаменатель безусловного hit-rate — 11 499 900 событий на held-out `rus_news_2024_1M`:
каждая позиция внутри предложения, чей ПРЕДЫДУЩИЙ токен прошёл `normalize_word`. Событие
без доступного предсказания считается промахом — поэтому число безусловное.

**Выбрано H = 10 000, K = 6** — то же, что у татарской таблицы. Обоснование:

* среди проходящих потолки наибольший безусловный hit-rate у H = 10 000 (13,64 % против
  12,98 %), и это единственное, что различает строки: внутри одного H величина от K не
  зависит вовсе (см. ниже, почему);
* при H = 10 000 потолки проходят K = 4 и K = 6, у них одинаковый hit-rate, и тай-брейк
  сделан в ту же сторону, что в E5b, — в пользу большего K;
* запас до потолков: сжатый 250 000 − 211 070 = **38 930 Б**, распакованный
  1 048 576 − 635 838 = **412 738 Б**. У татарской таблицы запас по сжатому был меньше —
  23 572 Б.

### Почему hit-rate не зависит от K, и что из этого следует

Внутри одного H все четыре K дают одно и то же число, и это не артефакт замера.
`TatBigrPrefixIndex.MAX_RESULTS = 3`: во время работы читаются **не более трёх** преемников
головы, полоса подсказок показывает ровно три ячейки, и `applyNextWordResult` берёт
`suggestions[0..2]` без всякой фильтрации. Преемники рангов 4–6 в файле есть, но прочитать
их сейчас не может ничто.

Цена этого для русской таблицы измерена: K = 6 против K = 4 — **+52 063 Б сжатого**
(211 070 против 159 007) и +130 070 Б распакованного, при полностью совпадающем
отображаемом результате.

Миссия **не** воспользовалась этим, чтобы урезать русскую таблицу до K = 4, и сознательно:
правило отсечки задано предыдущей фазой для обоих языков сразу, татарскую таблицу трогать
нельзя (перепаковка сменила бы её SHA-256 и заставила каждое устройство распаковать файл
заново), а разъехавшиеся K у двух языков — это ровно та асимметрия, которую потом никто не
объяснит. Наблюдение вынесено предложением в раздел 12; решать его стоит для обоих языков
сразу и не в этой миссии.

### Татарская таблица пересобирается байт в байт

Правка конвейера не изменила поведение генератора для татарского — и это не рассуждение
про значения по умолчанию, а прогон. Оба татарских архива скачаны заново, их SHA-256
сверены с `docs/DICTIONARY-E5B.md` и совпали (`c5a27c73…`, `de7816db…`), извлечённые
`*-sentences.txt` совпали с пинами `docs/DICTIONARY-E5A.md` (`838a10f0…`, `24318543…`), и
той же командой, что в 2026-08-17 (без `--language`, то есть по умолчанию татарский),
таблица собрана заново:

| | Перепаковка 2026-08-21 | Записано в E5b |
|---|---|---|
| Голов | 9 996 | 9 996 |
| Пар | 59 790 | 59 790 |
| Словарь преемников | 8 978 | 8 978 |
| Распакованных Б | 644 148 | 644 148 |
| Распакованный SHA-256 | `fb686476f6252f61f9d26632ccbd228f13aa1bffca7fe9bfff5f24baf9e0b05b` | тот же |
| Сжатых Б | 226 428 | 226 428 |
| Сжатый SHA-256 | `89eb4aa82be45a57ea94daa0379ca3d8a07f1c630e5c532960832787b1e1ab8d` | тот же |
| Выброшенные головы | `искәрткәнчә`, `билгеләвенчә`, `үтелгәнчә`, `толмацкий` | те же четыре |

`cmp` результата с `app/src/main/assets/bigrams/tatar_bigrams_v1.tatbigr.zlib` — без
различий. Перепакованный файл в репозиторий не клался, ассет 1.6.0 остался нетронутым.

---

## 6. Артефакт

`app/src/main/assets/bigrams/russian_bigrams_v1.tatbigr.zlib`, схема 2 / версия 1 — тот же
формат `TATBIGR\0`, что у татарской таблицы. Полный отчёт генератора —
`docs/RUSSIAN-BIGRAMS.pack.generated.json`.

| Свойство | Русская | Татарская (для сравнения) | Предел |
|---|---:|---:|---:|
| Голов запрошено | 10 000 | 10 000 | — |
| Голов в файле | **10 000** | 9 996 | — |
| Выброшено голов | **0** | 4 | — |
| Пар (P) | 59 982 | 59 790 | — |
| Словарь преемников (V) | 8 260 | 8 978 | — |
| Распакованных Б | 635 838 | 644 148 | 1 048 576 (запас 412 738) |
| Распакованный SHA-256 | `48f8ec6ab7bd262f847351a43633bde405d85aac82f8030b8b8f1de0a0491954` | `fb686476…` | точное совпадение |
| Сжатых Б | 207 608 | 226 428 | 250 000 (запас **42 392**) |
| Сжатый SHA-256 | `6ccb3ecc75d5c4d27d6f2b6b42a62628c3462ebd00a8e5fecffb9cdc87acb1ef` | `89eb4aa8…` | точное совпадение |
| Пиковый RSS генератора | 149 090 304 Б (≈ 142,2 МиБ) | 138 694 656 Б | — |
| Время упаковки | 263,459 с | 171,625 с | — |
| Шардов | 8 | 8 | — |

Русская таблица **меньше** татарской при той же конфигурации: словарь преемников на 718
слов короче (8 260 против 8 978), и русский текст сжимается плотнее.

**Ни одна голова не выброшена.** У татарской таблицы четыре слова из топ-10 000 не встретились
головой ни одной внутрисловарной пары; у русской таких нет — из 10 000 голов 9 992 несут полные
шесть преемников, четыре несут три, две — четыре, две — пять.

Оценка матрицы дала для этой конфигурации 635 838 Б распакованного (совпало ровно) и
211 070 Б сжатого против фактических 207 608. Расхождение только по сжатому и ожидаемое:
прототип E5a считает размер по формуле раскладки, а сжимает приблизительный байтовый образ
с другой шириной смещений — настоящий упаковщик пишет настоящую раскладку.

### Размер APK

Замер и сравнение — раздел 9.

### Хранение на устройстве

`bigrams-ru/russian_bigrams-ru-v000001-s2-f1-48f8ec6a….tatbigr`, каталог, семейство и
счётчик аренд — свои. Татарский `bigrams/tatar_bigrams-tt-v000001-…` не переименовывается и
не распаковывается заново; проверено на устройстве, раздел 8.

Новый каталог не требует нового правила бэкапа: `res/xml/data_extraction_rules.xml` и
`res/xml/backup_rules.xml` исключают домен `device_file` **целиком**, без единого
path-специфичного `<include>`, — это покрывает и каталог, которого не существовало, когда
правило писали. Ни один из двух XML не менялся.

---

## 7. Честный замер: что таблица предсказывает, а что нет

Досье предупреждало, что новостной перекос русских корпусов будет для биграмм сильнее, чем
для отдельных слов. Так и вышло. Ниже — измерение, а не оценка.

**Число из матрицы — новостное.** Безусловный top-3 hit-rate 13,64 % измерен на held-out
`rus_news_2024_1M`, то есть на новостях. Для новостного текста он честен; для переписки в
мессенджере он **завышает** пользу таблицы, и другого held-out нет: разговорного русского
корпуса у Лейпцига не существует, а подмешивать сторонний — решение оператора, не принятое.
Поэтому вместо второго hit-rate ниже приведено прямое наблюдение по словам.

### Новостной регистр предсказывается точно

| Голова | Первые три преемника |
|---|---|
| `в` | этом / том / **результате** |
| `по` | **словам** / его / **данным** |
| `связи` | **с** / со / и |
| `том` | **числе** / же / что |
| `течение` | нескольких / двух / трех |
| `рамках` | проекта / программы / реализации |
| `качестве` | примера / одного / главного |
| `соответствии` | **с** / со / критериям |
| `сообщает` | со / корреспондент / агентство |

«в связи с», «по данным», «в том числе», «в течение нескольких» — ровно то, ради чего
предсказание следующего слова и делается. Из 19 проверенных новостных слов головами
оказались 18 (не оказалось `течении`).

### Бытовой регистр — наполовину, и «половина» смещена

Из 32 проверенных бытовых слов головами таблицы оказались 17, а 15 отсутствуют целиком:
`привет`, `давай`, `ладно`, `слушай`, `извини`, `скучаю`, `целую`, `обнимаю`, `позвони`,
`напиши`, `приходи`, `купи`, `забыл`, `устал`, `голоден` — то есть почти все обиходные
императивы и все разговорные зачины.

Хуже того, у слов, которые в таблице есть, преемники тоже новостные:

| Голова | Все шесть преемников | Что это значит |
|---|---|---|
| `как` | и / в / можно / **сообщает** / раз / это | «как **сообщает**» — газетный оборот; «дела» нет вовсе |
| `дела` | в / по / о / и / с / об | «дела в компании», а не «как дела» |
| `хочу` | **поблагодарить** / сказать / **выразить** / быть / пожелать / поздравить | лексика официального выступления |
| `люблю` | и / свою / **тебя** / этот / эту / это | «тебя» есть, но третьим |
| `спасибо` | **за** / вам / всем / нашим / и / тебе | здесь совпадает с бытовым |

Практический вывод одной фразой: **«привет, как дела» таблица не предскажет ни в одном из
трёх переходов, а «в связи с» и «по данным» предскажет с первой ячейки.**

### У татарской таблицы этой дыры нет — но и она не полная

Для контраста та же проверка по татарской таблице (в её обучение входил `tat_web_2018_1M`):
из 26 бытовых слов головами оказались 17 — заметно больше доли, чем у русского (17 из 32),
и зачины разговора на месте: `сәлам`, `әйдә` («әйдә без **алып барыйк**»), `яратам`
(«яратам мин / һәм / **сине**»), `ярар`, `ничек`. Отсутствуют, однако, те же девять
императивов и состояний: `зинһар`, `сагындым`, `кил`, `кит`, `тыңла`, `онытма`,
`шалтырат`, `ашыйсы`, `арыдым`. Причина общая для обоих языков и не в корпусе: правило
«токен, отвергнутый нормализацией, рвёт соседство» систематически теряет последнее слово
каждой фразы, а императив в переписке чаще всего и стоит последним перед «!» или концом
строки.

**Это ограничение данных и правил подсчёта, а не дефект сборки.** Оно записано здесь
числами; варианты, что с ним делать, — в разделе 12, и ни один из них эта миссия не делала.

---

## 8. Проверка глазами на эмуляторе

AVD `tatar_e5_test` (Android 11 / API 30, x86-64, headless, `-gpu swiftshader_indirect`,
1080×2280, density 440), **release**-сборка этой ветки (R8 + `shrinkResources`), подписанная
тем же ключом, что 1.7.0 (сертификат SHA-256 `98ca6feb…`, keystore не трогался). Поле ввода
— приложение Messages. Скриншоты — в `docs/russian-bigrams/`.

Установка была **обновлением поверх 1.7.0**, а не чистой: это и есть проверка обратной
совместимости.

| Раскладка | Набрано | Полоса подсказок | Скриншот |
|---|---|---|---|
| татарская | `Бер мин ` | **дә / үзем / бу** | `light-tt-min.png` |
| русская | `В связи ` | **с / со / и** | `light-ru-svyazi.png` |
| русская | `Ну как ` | и / в / можно | `light-ru-kak.png` |
| русская | `Я привет ` | **пусто** | `light-ru-privet-empty.png` |
| татарская, тёмная тема | `Бер мин ` | дә / үзем / бу | `dark-tt-min.png` |
| русская, тёмная тема | `В связи ` | с / со / и | `dark-ru-svyazi.png` |

Каждая тройка совпадает с содержимым таблицы, прочитанным из ассета отдельно, — то есть
показывается именно то, что упаковано, а не что-то похожее.

**Переключение туда и обратно.** Татарская → русская → английская → татарская: на русской
приходят русские предсказания, на английской полосы нет вовсе (у `en_US` нет ни словаря, ни
таблицы), после возврата на татарскую снова `дә / үзем / бу`. Тёплый движок покинутого языка
отвечает сразу, без лишнего нажатия.

**Fail-closed виден на месте.** На русской раскладке после `Я привет ` полоса пуста — не
показаны ни татарские предсказания, ни какие-либо другие. `привет` не голова русской
таблицы (раздел 7), и правильный ответ на это — тишина.

### Обратная совместимость: файлы на устройстве

До установки (лежали с 1.7.0) и после установки + набора на обоих языках:

```
до:    bigrams/tatar_bigrams-tt-v000001-s2-f1-fb686476….tatbigr  644148  2026-08-21 04:16:41
после: bigrams/tatar_bigrams-tt-v000001-s2-f1-fb686476….tatbigr  644148  2026-08-21 04:16:41
после: bigrams-ru/russian_bigrams-ru-v000001-s2-f1-48f8ec6a….tatbigr  635838  2026-08-21 23:48:04
```

Татарская таблица сохранила и имя, и время — **она не распаковывалась заново**. Русская
появилась в своём каталоге при первом показе русской раскладки. Словари
(`dictionaries/`, `dictionaries-ru/`) тоже сохранили свои времена, 04:15 и 04:18.

### Одна деталь поведения, которая не является дефектом

Первые попытки набрать `мин ` в пустом поле предсказания не давали, и это правильно:
`TatarWordUtils.extractNextWordContext` возвращает пустую строку, если слово-контекст
упирается в границу кэша текста (строка `separatorStart - word.length == 0`). В пустом поле
первое слово в эту границу упирается всегда. Правило заведено до этой миссии и не менялось;
оно fail-closed — лучше не предсказать, чем предсказать по обрезанному контексту. Именно
поэтому все проверки выше набраны с предшествующим словом.

---

## 9. Числа

### Размер APK

| | Байт | Из 3 145 728 |
|---|---:|---:|
| 1.7.0 (артефакт релиза) | 2 386 361 | запас 24,14 % |
| эта сборка (release, подписанная) | **2 579 741** | **запас 17,99 %** |
| дельта | **+193 380** | −6,15 п.п. |

Ассет весит 207 608 Б, а APK вырос на 193 380 — уже сжатый zlib-поток кладётся в архив как
есть, без второго сжатия, и часть разницы съедает выравнивание.

Инвариант `BRIEF.md` (≤ 3 145 728 Б) **выполняется**, запас 565 987 Б. Но он сокращается
третий релиз подряд: 42,75 % → 24,14 % → 17,99 %. Это стоит знать той миссии, которая
захочет добавить следующий ассет.

В APK лежат ровно шесть файлов данных: `assets/bigrams/` — `russian_bigrams_v1.tatbigr.zlib`
207 608 Б, `tatar_bigrams_v1.tatbigr.zlib` 226 428 Б, `NOTICE.txt` 2 820 Б;
`assets/dictionaries/` — два словаря и свой `NOTICE.txt`.

### Холодный старт — инвариант выполняется

Метрика и стенд те же, что в `docs/APK-AUDIT-1.7.0.md`: старт процесса IME
(поле 22 `/proc/<pid>/stat`) → первый `FrameCompleted` окна IME (14-й столбец первой строки
`---PROFILEDATA---` в `dumpsys gfxinfo … framestats`). Перед каждым прогоном —
`sync; echo 3 > /proc/sys/vm/drop_caches`; показанная клавиатура убивается `kill -9`, и
система пересоздаёт процесс, так что старт процесса целиком относится к пути показа.

**Оба плеча сняты в одном сеансе, на одном эмуляторе, подряд** — для плеча 1.7.0 в тот же
процесс измерения ставился `dist/tatar-keyboard-1.7.0.apk`, затем возвращалась эта сборка.

| Прогон | эта сборка, мс | v1.7.0, мс |
|---:|---:|---:|
| 1 | 299,1 | 259,3 |
| 2 | 276,0 | 247,2 |
| 3 | 241,4 | 261,4 |
| 4 | 269,5 | 273,0 |
| 5 | 248,6 | 281,6 |
| 6 | 265,6 | 264,3 |
| **медиана** | **267,6** | **262,9** |
| размах | 241,4–299,1 | 247,2–281,6 |

**Инвариант 400 мс выполняется: худший прогон 299,1 мс, запас 100,9 мс (25,2 %).**
Разница медиан +4,7 мс при размахе внутри плеч 57,7 и 34,4 мс — диапазоны перекрываются
полностью, различить сборки этим замером нельзя. **Вторая таблица биграмм холодный старт не
удлинила**, и это ожидаемо по построению: хранилище таблицы создаётся лениво, при первой
подготовке, на фоновом исполнителе, и ни один ассет не читается на UI-потоке при показе.

Замер сделан на эмуляторе. Пункт чеклиста «Cold start < 400 ms» им **не закрывается** — он
требует реального устройства.

### Память

A/B в одном процессе, тот же приём, что в `docs/RUSSIAN-DICTIONARY.md` разделе 4: сначала
прогрет только татарский (движок + таблица), затем переключение на русскую раскладку
прогревает второй движок и вторую таблицу. Три замера подряд в каждой точке, значения
устойчивые.

| | PSS Total | «Other mmap» PSS | «Other mmap» Private Clean |
|---|---:|---:|---:|
| только татарский движок + татарская таблица | 50 444 КБ | 3 160 КБ | 3 052 КБ |
| оба движка, обе таблицы | 55 934 КБ | 6 260 КБ | 6 152 КБ |
| дельта | **+5 490 КБ** | **+3 100 КБ** | **+3 100 КБ** |

Дельта отображённых файлов **сходится с размерами артефактов до килобайтов**: русский
словарь 2 540 622 Б (2 481 КБ) + русская таблица 635 838 Б (621 КБ) = 3 102 КБ против
измеренных 3 100 КБ. То есть второй язык стоит ровно своих двух файлов, и **доля таблицы в
этом — 621 КБ**.

Это **чистые файловые страницы** (Private Clean): они вытесняются под давлением памяти и
подгружаются обратно, в отличие от кучи. Резидентными становятся только реально прочитанные
страницы — `predict` читает три идентификатора преемников, а не всю таблицу.

Для сравнения, `docs/RUSSIAN-DICTIONARY.md` мерил тот же переход на 1.7.0 (тогда у русского
не было таблицы) и получил «Other mmap» +2 484 КБ. Сегодня +3 100 КБ; разница ≈ 616 КБ —
это и есть добавленная русская таблица.

### Тесты

| Набор | Было (1.7.0) | Стало |
|---|---:|---:|
| JVM, тестов | 798 | **807** |
| JVM, suite-файлов | 80 | **81** |
| падений | 0 | **0** |

Девять новых тестов — `SuggestionsControllerBigramLanguageSwitchTest`, по образцу
`SuggestionsControllerLanguageSwitchTest` из предыдущей миссии:

1. каждый поставляемый язык резолвит **свою** таблицу через единственный реестр, а `en_US`
   не резолвит ни словаря, ни таблицы;
2. таблица не может принадлежать языку, отличному от своего словаря, — проверено обходом
   всего `ALL`, так что третий язык покрыт заранее;
3. у каждой таблицы своё семейство, свой каталог, свой ассет; имя татарского файла
   зафиксировано посимвольно; шаблон одного семейства не совпадает с файлом другого ни в
   одну сторону;
4. переключение раскладки переключает, какая таблица прикреплена, и татарскому движку
   русская не достаётся;
5. **seam таблицы всегда спрашивается для того же subtype, что seam словаря** — списки
   запрошенных subtype сравниваются поэлементно;
6. переключение обратно переиспользует тёплую таблицу и ничего не готовит дважды;
7. язык без таблицы молчит и **не занимает** таблицу соседа, а подсказки по префиксу у него
   работают;
8. отказ подготовки одной таблицы не отцепляет другую;
9. таблица, опубликованная позже, прикрепляется к своему движку, когда придёт.

Python: все шесть наборов зелёные (`tests/bigram_pack`, `tests/bigram_asset_pack`,
`tests/dictionary_pack` — 31 тест, было 29 плюс расширенный сторож лицензионных исходников,
`tests/dictionary_coverage`, `tests/emoji_pack`, `tests/typo_pack`).

### Приватность и lint

* `scripts/check-no-internet.sh` на release-APK — **exit 0**; единственное разрешение —
  `VIBRATE`; backup закрыт как whitelist (`allowBackup=false`, оба издания правил, ни одного
  `<include>`, все домены исключены).
* `./gradlew :app:lintVitalRelease --rerun-tasks` — **BUILD SUCCESSFUL**, задача выполнялась,
  не из кэша.
* Таблицы целиком локальные: ассеты лежат в APK, распаковываются в device-protected storage,
  сеть не используется нигде. Разрешения INTERNET в проекте нет.

---

## 10. Строка настройки теперь говорит правду

`tatar_suggestions_summary` — «Show suggested and **predicted** words above the keyboard as
you type, in Tatar **and in Russian**» — и `tatar_suggestions_offer_message` обещали
предсказание для обоих языков, а имели его у одного (раздел 1). Обещание закрыто первым из
двух способов, названных в досье: предсказание появилось, и **строки не менялись** — ни в
en, ни в ru, ни в tt.

Отсюда следует, что новых строк интерфейса нет и в `docs/TATAR-REVIEW-QUEUE.tsv` добавлять
нечего; очередь вычитки остаётся пустой от `pending`, какой её оставил оператор коммитом
`011bc11`.

Оговорка, которую честно сделать здесь: строка обещает предсказание, и оно есть, но
**качество** предсказания у русского новостное (раздел 7). Строка ничего не обещает про
регистр, так что расхождения между обещанием и поведением больше нет; расхождение между
поведением и тем, как человек пишет в мессенджере, — есть, измерено и вынесено в раздел 12.

---

## 11. Границы, которые соблюдены

* **Версия не поднята** — `build.gradle` не тронут, остаётся 1.7.0 / versionCode 10.
  `CHANGELOG.md` не менялся, `metadata/en-US/changelogs/` не менялся, в `dist/` ничего не
  клалось. Релиз собирает отдельная миссия.
* **Наружу ничего не отправлялось**: ни `git push`, ни тег, ни Release, ни merge. Работа
  велась локальными коммитами в `codex/version-1.6.0`. Загрузка корпусов Лейпцига — это
  получение входных данных, а не публикация.
* **Ветки предыдущих релизов не переписаны**; коммиты `4283944`, `b0980a8`, `652e1da` и
  frozen commit 1.7.0 `b2e2f33` на месте.
* **Keystore не создавался и не менялся.** Release-сборка подписана существующим ключом от
  2026-08-18, сертификат SHA-256 `98ca6feb…` — тот же, что у 1.7.0.
* **Разговорный корпус не подмешивался** — решение оператора не принято, вопрос открыт.
* **Отсечка словаря не менялась** (полные 100 000 форм), «оба словаря сразу» не делалось.
* **Татарская таблица не перепаковывалась** — ассет 1.6.0 остался нетронутым, его SHA-256 не
  изменился; контрольная перепаковка велась во временный каталог и в репозиторий не клалась.
* **Закрытые темы не переделывались**: панель эмодзи, поиск, кожные тона, край `?123`, нижний
  ряд, порог касания 8dp, устройство выбора словаря.
* **Лицензионные исходники в репозиторий не попали** — ни архивы, ни `*-sentences.txt`; тест
  теперь сторожит и их.
* Инварианты `BRIEF.md` не нарушены: APK 2 579 741 ≤ 3 145 728, разрешения INTERNET нет,
  холодный старт 299,1 мс худший при пределе 400, цикл отрисовки не трогался вовсе.

---

## 12. Что найдено сверх темы и что предлагается

Всё ниже — наблюдения и предложения. **Ничего из этого миссия не делала.**

1. **Преемники рангов 4–6 не читает ничто.** `TatBigrPrefixIndex.MAX_RESULTS = 3`, полоса
   показывает три ячейки, фильтрации между ними нет. Для русской таблицы K = 6 против K = 4
   — это 52 063 Б сжатого и 130 070 Б распакованного при полностью совпадающем отображении;
   у татарской пропорция та же. Решать стоит **для обоих языков сразу**, потому что
   перепаковка татарской таблицы меняет её SHA-256 и заставляет устройства распаковать файл
   заново — то есть это работа релизной миссии, а не отдельная правка. Вариантов два:
   оставить как есть (запас на случай, если ячеек когда-нибудь станет больше) или ужать обе
   таблицы до K = 4 и вернуть себе ~100 КБ APK.
2. **Разговорного регистра в русских данных нет** (раздел 7) — то же ограничение, что у
   словаря, только заметнее. Варианты те же: подмешать открытый разговорный корпус (у
   OpenSubtitles своя лицензия — отдельное решение) либо не делать ничего и не обещать
   бытовое предсказание. Решение за оператором.

> **Обновление 2026-08-24.** Отдельное решение принято: оператор разрешил использовать
> OpenSubtitles, зная, что лицензионного гранта у источника нет вообще. Замеры, принятый
> риск и очереди приёмки — `docs/CORPUS-OS.md`; риск при публикации — отдельный раздел
> `docs/PUBLISH-CHECKLIST.md`. Текст выше не переписан.

3. **Правило «отвергнутый токен рвёт соседство» теряет последнее слово каждой фразы.**
   Это заметно бьёт именно по императивам («позвони!», «приходи»), которых в переписке
   много. Правило заведено в E5a и проверено отдельно; менять его — значит пересобрать обе
   таблицы и заново обосновать выбор. Стоит один раз посчитать, сколько пар прибавится, если
   считать соседство через одиночный знак препинания в конце предложения.
4. **Запас APK сокращается третий релиз подряд** (42,75 % → 24,14 % → 17,99 %). Вместе с
   уже открытым у оператора вопросом про целевую планку в `BRIEF.md` (2,46 МиБ фактически
   против записанных 1,7 МиБ) это стоит решить до следующего ассета, а не после.
5. **`docs/DICTIONARY-E5B.md` ссылается на `AtomicBigramStore.FINAL_FILE_PATTERN`**, которой
   больше нет: шаблон переехал в `BigramArtifactSpec.finalFilePattern`, потому что он теперь
   зависит от семейства. Документ — запись приёмки от 2026-08-17, и миссия его не правила.
6. **Холодный старт и PSS на реальном телефоне по-прежнему не измерены** — в проекте это так
   с самого начала. Здесь оба замерены на эмуляторе; телефона это не заменяет.
7. **TalkBack не проверялся** — на образе `tatar_e5_test` он не установлен. Шестой отчёт
   подряд с этой записью. Полоса подсказок теперь может показывать русские предсказания;
   озвучка ячеек не менялась, но и не проверялась.
8. **«Оба языка сразу»** остаётся возможным и для таблиц: слоты держат оба движка тёплыми,
   обе таблицы прикреплены каждая к своему движку. Недостающая часть та же, что у словарей,
   — правило приоритета кандидатов при конкуренции.

---

## 13. Что осталось оператору

1. **Бэкап keystore вне этой машины** — восьмой прогон подряд невыполненным.
2. **Проверка на телефоне**: русские предсказания после набранного слова, переключение
   раскладки туда-обратно, холодный старт, TalkBack.
3. **Решения, которые ждут оператора** — пункты 1, 2 и 4 раздела 12: отсечка K для обеих
   таблиц, разговорный корпус, целевая планка размера APK.
4. Релиз с этой работой собирает отдельная миссия — версия здесь не поднималась
   сознательно.

---

STATUS: done
