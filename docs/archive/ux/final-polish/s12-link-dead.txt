tt-final, находка 3: строка «Privacy Policy» без браузера.

AVD tatar_e5_test, API 30. Браузер выключен командой
    adb shell pm disable-user --user 0 com.android.chrome
    adb shell cmd package resolve-activity -a android.intent.action.VIEW -d https://example.com
    -> No activity found

ДО правки — подписанная 1.8.3 (org.tatarkeyboard.ime), тап по строке «Privacy Policy»:
    экран не изменился, иерархия та же, диалога нет;
    единственный след — logcat:
        08-24 00:12:37.032 28788 28788 E SettingsHostActivity: Browser not found
    окон Toast на экране: 0, 0, 0 (три замера подряд, dumpsys window windows)

ПОСЛЕ правки — сборка с правкой (org.tatarkeyboard.ime.debug), тот же тап:
    Window #4 Window{e6f533c u0 Toast}:
      mOwnerUid=10157 package=com.android.systemui appop=TOAST_WINDOW
      mAttrs={(0,66)(wrapxwrap) gr=BOTTOM CENTER ... ty=TOAST ...}
    (окно Toast присутствует в трёх замерах подряд)
    строка E SettingsHostActivity: Browser not found на месте — она для разработчика,
    Toast для человека.

Toast не попадает ни в uiautomator dump, ни в screencap экрана настроек (этот экран
отдаёт нулевые файлы), поэтому свидетельство снято через dumpsys window windows.
