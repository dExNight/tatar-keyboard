# FINAL-AUDIT-2026-09-02 — финальный предрелизный аудит 1.9.11

Дата: 2026-09-02. Объект: `dist/tatar-keyboard-1.9.11.apk` (1 792 011 Б,
SHA-256 `5e112f65…bdd4`, versionCode 27) и дерево main `7e9856b9`.
Четыре независимых аудита: безопасность, баги, производительность, полнота
функций/релиз-готовность. Этот файл — единый реестр всего, что требуется
доделать/исправить/реализовать, в порядке приоритета.

## Вердикт

Технически релиз готов: release_check 12/12 PASS, 1 063 JVM + 346 python-тестов
зелёные, критических и высоких проблем не найдено ни в одном аудите.
**Публиковать в сторы нельзя из-за витрины (metadata): скриншоты показывают
другое приложение** (унаследованы от Simple Keyboard, демонстрируют
несуществующие фичи).

---

## A. Блокеры публикации

| # | Что | Детали |
|---|---|---|
| A1 | **Переснять все скриншоты metadata** (9 файлов: phone/7″/10″) | Текущие — от Simple Keyboard: английская QWERTY, «Dynamic colors», «Custom colors» — фич, которых нет в приложении. Ни один не показывает пятый ряд ә ө ү җ ң һ, подсказки, эмодзи-панель. Вводящая в заблуждение витрина — сторы такое отклоняют. То же: `images/screenshot-0.png` |
| A2 | **Разделы 5–7 PUBLISH-CHECKLIST** (действия оператора) | push в публичный репо (сейчас 404 анонимно), зелёный CI на frozen commit, тег v1.9.11, GitHub Release с APK строго из dist/, IzzyOnDroid request (AI-раскрытие готово текстом, чек-лист строки 1580–1593 — проверить актуальность списка инструментов) |
| A3 | **ru-локаль metadata** (минимум title/short/full + changelog 27) | RuStore — приоритетный канал по брифу, аудитория русскоязычная; tt — желательно |

## B. Важное (до или сразу после публикации)

| # | Что | Severity | Детали |
|---|---|---|---|
| B1 | **Обновить `full_description.txt`** | medium | Описание эпохи 1.2.0: нет предсказаний следующего слова, русских подсказок, эмодзи-панели/поиска/подсказок, личного словаря. Продаёт треть продукта |
| B2 | **Обновить PRIVACY.md** (v1.3, 2026-07-31) | medium | (а) не описаны карантинные файлы `*.tpers.quarantine` (долг с 1.8.2); (б) размеры распакованных данных устарели (schema 2/3 + ретенция двух версий); (в) эмодзи-подсказки не упомянуты |
| B3 | **Устаревший baseline-профиль** | low-med | Регенерирован в SIZE-3 **до** эмодзи-подсказок и фикса гонки: 0 вхождений `EmojiSuggest*`, нет `onBigramAttached` и двухаргументного `extractNextWordContext`. Лечится: `./gradlew :app:generateReleaseBaselineProfile` на эмуляторе; CUJ стоит дополнить включением тоггла эмодзи-подсказок |
| B4 | **Страж `onBigramAttached` пропускает перезапрос, если полосу занял эмодзи/companion** | low | ~~`SuggestionsController.kt:1135`~~ **Закрыто 2026-09-02** (миссия CODE-FIX, коммит `f32f3525`): страж проверяет «есть словесная ячейка АКТИВНОГО языка» (`bandHasActiveLanguageWord`), а не занятость полосы; attach companion-слота перезапрашивает companion-заполнение при свободной ячейке. +4 JVM-теста (красный→зелёный: без фикса 2 failed), эмулятор: 3/3 холодных прогона «йөрәк » → полоса [өянәге · авыруыннан · ❤️] без лишнего нажатия (`build/codefix-evidence/`) |
| B5 | **featureGraphic для Play** | low | `icons/play_feature.png` есть, в fastlane не проложен |

## C. Мелкие исправления (можно одной миссией)

| # | Что | Где |
|---|---|---|
| C1 | Пароль keystore виден в `ps` при подписи → `--ks-pass env:VAR` вместо `pass:` | `scripts/release_pack.sh:124–128` |
| C2 | `release_check.sh`: список эмодзи-ассетов зашит — новый файл не покроется (fail-open). Сверять множество файлов APK↔дерево | `scripts/release_check.sh:317` |
| C3 | `rebuild_assets.py`: ContractError даёт код 1 вместо обещанного 2 | `scripts/rebuild_assets.py:438–449` |
| C4 | `emulator-smoke.sh`: пустой `wm size` роняет скрипт без RESULT-строки; run-as затирает префы целиком (задокументировать) | `scripts/emulator-smoke.sh:159,247–253` |
| C5 | ~~Диалоги (AlertDialog «забыть слово») вне `filterTouchesWhenObscured`~~ **Закрыто 2026-09-02** (CODE-FIX, `0676514a`): `DialogUtils.filterObscuredTouches` ставит флаг на decorView по OnShowListener; покрыты все 8 диалогов LatinIME, выбор раскладки, 6 диалогов настроек, seek-bar диалог. Контракт-тест `DialogObscuredTouchContractTest` пинит разводку; эмулятор: диалог «Forget "өтәлкүл"?» показан, Delete отработал, слово забыто | ~~`LatinIME.java:887`~~ |
| C6 | ~~Инвариант «кэш < 1024 ⇒ начало текста» опровержим~~ **Закрыто 2026-09-02** (CODE-FIX, `6efae5b0`): явный флаг происхождения `RichInputConnection.mCacheReachedTextStart`, пишется только полным перечитыванием (`onBeforeCursorCacheReloaded`), локальные мутации его СОХРАНЯЮТ (они двигают только курсорный край окна — см. коммит: сброс на commitText убил бы предсказание первого слова). Читатели (LatinIME, InputLogic) переведены на `cacheReachedTextStart()`. +5 JVM-тестов (красный→зелёный на старом выводе по длине); эмулятор: «мин » → дә·аны·бу, свайп курсора туда-обратно, предсказание вернулось | ~~`RichInputConnection.java:503–510`, `LatinIME.java:529–531`~~ |
| C7 | ~~Дубль `EmojiSearchIndex` в памяти~~ **Закрыто 2026-09-02** (CODE-FIX, `663c27df`): `SharedEmojiSearchIndex` — процессный держатель одного разбора `emoji_search_v1.txt` (ленивый, synchronized, fail-closed, EMPTY кэшируется); suggest-источник и панель читают его. +3 JVM-теста (идентичность экземпляра, терминальность провала, гонка парсит раз) | ~~`EmojiSuggestSources.kt` ↔ `EmojiPanelController.kt:186`~~ |
| C8 | `tatar-keyboard-release.jks` вынести из корня репозитория (действие оператора) | корень |

## D. Не сделать нельзя, но не кодом (оператор/железо)

| # | Что | Статус |
|---|---|---|
| D1 | Замеры на живом устройстве: PSS, холодный старт, MIUI-убийца процесса | ни разу не мерены; по коду регрессий нет |
| D2 | Тестовая матрица брифа: Telegram, Chrome/WebView, ландшафт, TalkBack руками | ни разу; только эмулятор + JVM |
| D3 | Разговорный корпус: 2 русских слова молчат (окей, берегись), tat 3/0 деепричастия | осознанный остаток |
| D4 | Вариант B эмодзи-подсказок (под курсором без пробела) | отложен по плану, оценить спрос после публикации |
| D5 | Lossy-ужатие ~190 КБ (u8-log2 частоты) | отдельное продуктовое решение |

---

## Проверено и чисто (эксплицитно)

- **Безопасность**: IPC-поверхность минимальна (одно разрешение VIBRATE,
  exported-активности не читают extras), ноль сетевого кода, парсеры schema 2/3
  fail-closed с пинами SHA-256 и связкой таблица↔словарь, гейты приватности
  (пароли, NO_PERSONALIZED) наследуются эмодзи-подсказками, логи чисты,
  хранилища разделены device/credential-protected, карантин, backup-whitelist.
- **Корректность**: эквивалентность schema 2/3 доказана полными сверками
  (76 839 префиксов; 20 202 головы); фикс гонки покрыт красный/зелёным тестом.
- **Производительность**: latency читателей v2/v3 в бюджетах с запасом 2–3
  порядка (худший 0,102 мс при кадре 16,7 мс); ноль аллокаций в hot path;
  mmap сохранён; потоки ограничены и завершаются; APK без мусора.
- **Полнота**: все 7 пунктов MVP-брифа реализованы; из v1+ сделано всё
  заявленное кроме «истории буфера» (не заявлена).
